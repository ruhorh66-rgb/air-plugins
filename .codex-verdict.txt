BLOCK: Автопродолжение не покрывает все заявленные терминальные исходы.

- `approve_listener.py:240–272, 294–297`; `SKILL.md:826–833` — ранние терминальные отказы (невалидные пути/параметры, неверная подпись, таймаут ожидания) возвращаются без `_push_next()`, хотя документация обещает его для любого терминального исхода. Воспроизведение: подтвердить заявку при изменённом после выдачи файле цели и иметь следующую `queued`-задачу; следующая кнопка автоматически не придёт. Серьёзность: высокая.