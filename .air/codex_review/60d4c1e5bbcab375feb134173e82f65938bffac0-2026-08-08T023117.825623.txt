ALLOW: гейт запуска роя через Telegram — blocker с инъекцией PowerShell закрыт (-File + argv вместо -Command), 4 minor закрыты, ложно-зелёный тест переписан; 6 из 6 проверок проходят. Codex, два прохода, 08.08.2026.

CODEX REVIEW — air-ruflo-bridge, гейт подтверждения запуска роя через Telegram
Рецензент: Codex (GPT-5.6, ур.4), два прохода, 08.08.2026
Предмет: E:\-7-\air-ruflo-bridge\skills\run-via-ruflo\scripts\
  approve_via_telegram.py, approve_listener.py, run_task.ps1, test_approve_queue.py
Коммиты: b7f167d, fb9a068, bcd9e50 (+ доработка по второму проходу)

=== ПРОХОД 1 — вердикт BLOCK ===

[blocker] approve_listener.py:146 — `report` из заявки без валидации вставляется
  в PowerShell-строку `-Command`; значение с `"` и `;` позволяет выполнить
  произвольный PowerShell при подтверждении. Передавать параметры отдельными
  аргументами через `-File` и валидировать `report`.
[major] approve_via_telegram.py:248 — `body[:3500]` молча отрежет часть списка
  вопреки требованию показывать все нерешённые.
[major] approve_via_telegram.py:137 — один битый JSON прерывает `create()` после
  записи заявки, но до отправки кнопки; заявка повисает нерешаемой.
[minor] approve_listener.py:234 — решение перезаписывается неатомарно.
[minor] approve_via_telegram.py:168 — сводка уходит после кнопки, хотя
  комментарий утверждает обратное.
[minor] approve_via_telegram.py:144 — неограниченный `title` способен сделать
  сообщение недоставляемым.

ИТОГ ПРОХОДА 1: BLOCK

=== ПРОХОД 2 — после исправлений, вердикт ALLOW ===

1. closed — исполнение теперь argv-список, не интерполированный `-Command`:
   `subprocess.run(argv, ...)`, approve_listener.py:171-183. CLI_PATH — литерал
   в исходнике (118-120); из заявки приходят только пути, проверенные
   `_safe_path` (131-133, обязателен абсолютный путь), целое число воркеров и
   приоритет из белого списка. Python сохраняет элементы последовательности как
   отдельные аргументы, а PowerShell `-File` трактует следующие значения как
   литеральные аргументы скрипта, а не как выражения — пробелы и спецсимволы
   не расщепляют и не переразбирают принятое значение. Отдельный запрет ведущего
   дефиса не требуется: пути обязаны быть абсолютными.
   `-ObjectiveFile` читается до проверок CLI/target (run_task.ps1:47-50 против
   71-72), это литеральное чтение файла, не вычисление выражения; прежний путь
   вызова с `-Objective` работает без изменений.
2. closed — нерешённые больше не теряются молча (266-277): сначала вытесняются
   завершённые, затем нерешённые с явным счётчиком. Худший случай тела — 3588
   символов при лимите 4096. Оговорка рецензента: семантика подсчёта не-BMP
   символов на стороне Telegram отдельно не проверялась.
3. closed — `create()` пропускает битый и не-объектный JSON (148-154).
4. closed — обе записи через `os.replace` (approve_via_telegram.py:118-121,
   approve_listener.py:267-270).
5. closed — сводка отправляется перед сообщением с кнопками (171-183).
6. closed — title ограничен при создании ([:120]) и при выводе ([:80]).

Новые находки прохода 2 (все minor), закрыты после рецензии:
- queue() принимал синтаксически валидный не-объектный JSON и падал на
  `d.get(...)` — добавлена проверка `isinstance(d, dict)`;
- test_done_trimmed_before_pending был ЛОЖНО-ЗЕЛЁНЫМ: бюджет не исчерпывался,
  тест проходил и с удалённым циклом обрезки — переписан так, что нерешённые
  занимают почти весь бюджет, добавлена проверка вытеснения завершённых;
- добавлен тест на не-объектный JSON.
- Не закрыто (принято как остаточное): `report` не ограничен по длине при
  записи заявки — предельно длинное значение может сделать сообщение
  недоставляемым. Значение формируется вызывающей сессией, не приходит извне.

ИТОГ ПРОХОДА 2: ALLOW

Проверки исполнителя после правок: test_approve_queue.py — 6 из 6 пройдено;
run_task.ps1 — BOM 239,187,191 на месте, парсер чист, оба режима цели
(-Objective / -ObjectiveFile) проверены запуском.