# AirWorker 0.10.9 release acceptance — 2026-09-18

Scope: urgent DevSpace operational hotfix, canonical PLAN steps 91/92/91а.

- Diagnosis: the 0.10.8 Codex executor and orchestration leader already used `workspace-write`. A deterministic installed-binary shim captured `-s workspace-write`, model `gpt-5.6-luna`, effort `medium`, and created a file in the product root. The planner, semantic reviewer and orchestration subagents correctly used `read-only`.
- Actual DevSpace blocker: PowerShell selected checks received the literal `-Select` as a positional value because AirWorker quoted the parameter token. Four checks therefore reported `unknown selector: -Select` before product work could be assessed.
- Fix: selected PowerShell parameter names are passed as parameter tokens while selector values remain quoted. A process-level regression test covers ordinary and leading-hyphen values. Codex and Claude planner, orchestration subagent and leader receipts show role and sandbox; semantic record and console show reviewer role and sandbox. The Codex roster is printed before launch, and the generic Claude permission line is explicitly labeled.
- DevSpace acceptance: source-built 0.10.9 judge ran without an LLM. `К1` passed; no `unknown selector: -Select` appeared. Four checks failed on concrete missing product evidence, including `test-product/AirTunnelConfig.json`, `benchmark/PerformanceBudget.json`, `device-acceptance/SecurityBoundaries.json`, and `release/SemanticReview.json`. Factual verdict remains red until DevSpace supplies evidence; AirWorker transport no longer blocks the cycle.
- Independent core review: AirWorker orchestration launched two `gpt-5.6-luna`/medium read-only subagents. Both completed with receipts carrying `role=orchestration-subagent`, `sandbox=read-only`. The leader was launched with `role=executor/leader`, `sandbox=workspace-write` and wrote a `FIXED` review marker. The local factual judge passed 1/1; the leader process was interrupted after producing the marker because it continued unrelated environment diagnostics, so its receipt does not claim DONE. Reviewers identified a leading-hyphen selector edge case and a missing Claude planner receipt; both were corrected before final test.
- Test gate: `TestPowerShellSelectorParameterReachesScript` PASS, full `go test ./... -count=1` PASS (main and tray), `go vet ./...` PASS, `git diff --check` PASS. Plugin manifest/hook check PASS at 0.10.9; goal-file 28/28 PASS; reproducibility 40/40 PASS; canonical goals 7/73 and valid. CLI rebuilt with Go and returns 0.10.9; SHA-256 `7FEEACADE7D30841CE6FCD382500D4A380350A1CE514E2FE836E7B302A9B4BC3`.
- Publication: PASS. Release commit `3a1df0d4a3ae0dc7e8b28c611db3322df2dc7dfa` is on origin/main; annotated tag `air-worker--v0.10.9` is published and peels to that commit.
- Distribution/install: PASS. Claude marketplace/plugin and Codex marketplace/plugin report 0.10.9 from GitHub `ruhorh66-rgb/air-plugins`. Source, Claude cache, Codex cache and live CLI all have SHA-256 `7FEEACADE7D30841CE6FCD382500D4A380350A1CE514E2FE836E7B302A9B4BC3`. User-scope `install -no-start` succeeded without UAC; Start Menu/PATH registered, autostart absent, tray stopped; `install -status` reports no source violations.
- Rollback: `air-worker--v0.10.8` through the canonical GitHub marketplace and user-scope `install -no-start`.
