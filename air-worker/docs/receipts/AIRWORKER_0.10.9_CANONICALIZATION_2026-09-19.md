# AirWorker 0.10.9 — канонизация, 19.09.2026

Основание — указание ЛПР 19.09.2026: «разрабатывать новый релиз не твоя задача. Твоя задача только навести порядок, всё подтянуть, чтобы новые сессии, которые загружают 10.9, у них всё было однозначно»; 0.10.10 и Hermes не трогать. Исполнитель — Claude Code (Opus 5) на SRVLM01. Удаления и push выполнены по явному разрешению ЛПР в той же сессии. Код продукта не менялся, новый выпуск не делался.

## 1. Перепроверка выпуска 0.10.9

Чистая копия тега `air-worker--v0.10.9` → `3a1df0d4a3ae0dc7e8b28c611db3322df2dc7dfa` во временном worktree, удалённом после прогона.

| Проверка | Итог |
|---|---|
| версия `plugin.json` | 0.10.9 — PASS |
| SHA-256 `bin/air-worker.exe` = эталон `7FEEACADE7D30841CE6FCD382500D4A380350A1CE514E2FE836E7B302A9B4BC3` | PASS |
| `git diff --check` релизного коммита | PASS |
| `go vet ./...` | PASS |
| `go test ./... -count=1` | PASS: `airos/air-worker` 60,3 с, `airos/air-worker/tray` 0,4 с |
| `tools/check-plugin.ps1` | PASS, 14 из 14 |
| `tools/check-goal-file.ps1` | PASS, 28 из 28 |
| `tools/check-reproducible.ps1` | PASS, 40 из 40 |
| `tools/run-selftest.ps1` | PASS, 36 из 36 |
| `air-worker goals` | годен: 7 целей, 73 критерия |
| `tools/check-binary.ps1` | **FAIL, 3 проверки**: вердикт legacy-судьи `judge.ps1` не содержит полей `criteria_passed`, `criteria_total`, `lpr_gates` и текста «критериев N», которые есть у бинарника. В релизный гейт 0.10.9 по акту приёмки не входила; записана кандидатом `FB-20260919T044427Z-5a14b9d6` |

Установленное на SRVLM01: кэш плагина Claude 0.10.9, кэш плагина Codex 0.10.9, live CLI `%LOCALAPPDATA%\air-worker\bin\air-worker.exe` — SHA-256 равен эталону; `air-worker install -status` — «нарушений источника нет».

## 2. Каноническое дерево `F:\-7-`

До: локальный `main` на `ccab03c` — air-worker 0.10.6 (`db5708d`) плюс коммит site-builder, 11 коммитов позади `origin/main`. В `air-worker/` — 5 изменённых и 15 неотслеживаемых файлов: пачка шагов 41/46, её receipts и handoff, 4 feedback по 0.10.9.

Сохранено до очистки: локальная ветка `wip/air-worker-0.10.10-batch-41-46` → `a050388`, 23 файла. Полнота сверена побайтно: `git hash-object` каждого неотслеживаемого файла равен blob снимка, у отслеживаемых расхождений со снимком нет.

Сделано: `git restore` и `git clean` только по `air-worker/`, затем `git reset --keep origin/main`. `ccab03c` совпадал с `cedb2f6` на origin по patch-id, потерь нет. 285 незакоммиченных файлов соседних продуктов (site-builder, air-ruflo-bridge) сверены до и после по SHA-256 — без изменений.

После: `HEAD = origin/main`, `git status --short -- air-worker` пуст, `plugin.json` 0.10.9, SHA бинарника равен эталону.

## 3. Хвосты

Убраны 11 перечисленных ниже worktree и папка-фикстура; каждый объект сохранён вместе с незакоммиченным состоянием в `refs/archive/air-worker-worktrees-20260919/<имя>`. Два worktree 0.10.10 оставлены — см. «Не тронуто».

| Имя | Что это | Архив |
|---|---|---|
| `aw-haiku-real-cohere` | smoke 0.10.5, `0986247` | `c01a933` |
| `aw-haiku-real-deepseek` | smoke 0.10.5, `0986247` | `3cf597b` |
| `aw-haiku-real-nex` | smoke 0.10.5, `0986247` | `fd9ee7d` |
| `aw-router-cohere` | smoke 0.10.5, `0986247` | `1312456` |
| `aw-router-poolside` | smoke 0.10.5, `0986247` | `37628d6` |
| `aw-router-smoke-a` | smoke 0.10.5, `0986247` | `c645286` |
| `aw-router-smoke-b` | smoke 0.10.5, `0986247` | `127b088` |
| `aw-step62` | шаг 62, в main | `4a5585b` |
| `aw-step63` | шаг 63, в main | `5842ee1` |
| `aw-step65` | шаг 65, в main тем же patch-id | `177d661` |
| `aw-step66` | шаг 66, в main как `a950b73` | `0bce719` |
| `aw-e2e-2008` | папка-фикстура 14.09, не worktree | `04fd8f0` |

Локальные ветки удалены: `ai/air-worker-step62` и `ai/air-worker-step63` (`2711174`), `ai/air-worker-step65` (`3abf361`), `ai/air-worker-step66` (`58746c0`), `task-obs-0046-air-worker` (`6f6b500`) — содержимое в main предком или тем же patch-id.

Ветки GitHub удалены: `air-worker-openai-ladder-0.10.8` (`b9a1b48`) и `codex-add-dirs-0.10.6` (`dda362f`), обе — предки `origin/main`.

Не тронуто: `release/air-worker-v0.10.10-hermes`, `fix/semantic-reviewer-config`, `ai/air-worker-step38` и `ai/air-worker-step46-session-migration` с их worktree, `backup/pre-air-worker-*` (08.2026), `feature/air-worker-direct` (копия Codex, 08.2026).

Трей: второй `air-worker-tray.exe` — PID 12092 в сессии 0 с 18.09.2026 08:53. Без повышения `Stop-Process` ответил «Access is denied» (транскрипт сессии). После подтверждения ЛПР запроса UAC процесс остановлен с повышением 19.09.2026 13:19. По логу остановки его образ — `…\plugins\cache\air-plugins\air-worker\0.10.9\bin\air-worker-tray.exe`, SHA-256 `5ACD289E3CF7450C…`. Тот же SHA-256 у образа видимого значка PID 12556 в сессии 1 (`%LOCALAPPDATA%\air-worker\bin\air-worker-tray.exe`), он остался единственным. Кто поднял процесс в сессии 0, неизвестно: родитель к 19.09 уже не существовал. Кандидат `FB-20260919T044427Z-1c5cc3db`.

AIR_OS: постановки `AIR-CHG-2026-000091` и `AIR-CHG-2026-000092` лежали неотслеживаемыми; закоммичены отдельным коммитом `cfe3c5b`, содержимое не менялось. По согласованию ЛПР в том же цикле: указатель `F:\-0-\AGENTS.md` (вне git) ведёт в `00_BOOTSTRAP/AIR_START.md`, строка SRVLM01 в `AIR_VAULT_INDEX` записана фактом машины, в `ERROR_REGISTRY` внесены `ERR-2026-000292`…`000296` — разбор этих отказов с правилами.

## 4. PLAN и обратная связь

- Четыре feedback по 0.10.9 от 18.09.2026 перенесены из устаревшего дерева: evidence-файлы и строки кандидатов дословно из снимка.
- Восемь новых feedback этой сессии 19.09.2026: `5a14b9d6` — красная `check-binary`, `3154812f` — команды молча работали по отставшему дереву, `1c5cc3db` — трей в сессии 0, `62b60fbf` — `tray -status` против `install -status`, `4cb2c435` — два формата хода (решён: вариант А), `3ceaa2e8` — скил Вуди ссылается на загрузку через AIR_START, `44fa81f7` — `drift -json` отвечает текстом на негодном плане, `23872bd7` — ссылки на `docs/GOAL.md` за блокером `llm-queue`.
- Три записи соседних сессий, сделанные штатным `feedback` в каноническом дереве уже после синхронизации (`FB-20260919T052235Z-83ec8ec3` — планировщик Codex и «command line is too long», `FB-20260919T053219Z-db1b7314` — указание ЛПР передавать задания файлами, `FB-20260919T054925Z-03d8b601` — указание ЛПР: штатным запуском планировщика занимается ядро AirWorker), включены в коммит дословно: evidence-файлы побайтно, строки кандидатов из PLAN дерева.
- У раздела кандидатов записано правило: текущий статус — в строке PLAN, evidence-файл — неизменяемая запись приёма.
- Кандидаты 0.10.8 `FB-20260917T170402Z-51e49c4b` и `FB-20260917T165016Z-7d54dd40` помечены `closed`: их закрыл шаг 91 выпуска 0.10.9.
- Строка кандидата из `fix/semantic-reviewer-config` перенесена дословно; код ветки не сливался.
- В «Состояние и порядок старта» добавлены пункты «Канонизация 0.10.9» и «Что выпуском НЕ является»; шаг 3 порядка старта проверяет, что дерево каноническое.
- README получил шапку «Start here (0.10.x)»; handoff 15.09.2026 переложен из корня продукта в `docs/receipts/`.

## 5. Отчёт соседней сессии (Sonnet 5) при загрузке плагина, 19.09.2026

ЛПР передал его с указаниями «тоже почистить» и «проверь, что эти все хвосты по air-worker подтянуты». Разбор по пунктам:

| Замечание соседней сессии | Что сделано |
|---|---|
| README плагина описывает версию 0.3.0 | шапка «Start here (0.10.x)»; строка про 0.3.0 переписана как «Since 0.3.0 its default is…» — это дата умолчания исполнителя, а не версия продукта. В установленном кэше 0.10.9 README останется прежним до следующего выпуска: документы дерева опережают кэш |
| README vault'а — пустой шаблон | `AirWorker_Wiki\README.md` (вне git): шапка со входом — `PRODUCT_INSTRUCTION.md` и PLAN; текст шаблона сохранён ниже как есть |
| `DEPLOY.md` говорит про автозапуск, которого нет | `DEPLOY.md` сверен с кодом 0.10.9: `install` без `-autostart` автозапуск не объявляет, сам дописывает PATH и регистрирует ярлык в меню «Пуск»; `-uninstall` снимает автозапуск, ярлык и PATH; маркетплейс — только GitHub (шаг 66), не локальный клон; хуков 6 событий и 7 обработчиков, а не «5 хуков»; образец `install -status` заменён живым выводом 0.10.9 |
| AIR_START п. 6 ведёт в устаревшую копию Вуди (66 КБ против 73 КБ) | `E:\-5-\014_Skills\air-woody` расформирован 13.09.2026 (`fa60065`, `DISSOLVED.md`: «Правки сюда не вносятся»), действующий скил — `skills/woody/SKILL.md` плагина, SHA-256 равен кэшу 0.10.9. Решение ЛПР 19.09.2026: отдельным скилом Вуди не грузится, остаётся только в составе плагина air-worker. Пункт 6 снят (AIR_START 0.14 → 0.15, основание и прежний текст — в `AIR_START_JOURNAL.md`); вопрос «шести килобайт» этим снят. Упоминание AIR_START в самом скиле — кандидат `FB-20260919T052623Z-3ceaa2e8` на следующий выпуск |
| два процесса `air-worker-tray` | кандидат `FB-20260919T044427Z-1c5cc3db`; лишний процесс в сессии 0 остановлен с повышением прав, запрос UAC подтвердил ЛПР (раздел 3) |
| `tray -status` «значок поднят», а `install -status` — отказ оболочки | кандидат `FB-20260919T051153Z-62b60fbf` (со слов соседней сессии, эта сессия не воспроизводила) |
| два формата хода: Ядро п. 1.6 и отчёт `air-worker report` | кандидат `FB-20260919T051153Z-4cb2c435`; решение ЛПР 19.09.2026 — вариант А: строка состояния Ядра → отчёт → «Дальше / От тебя жду». `docs/INTERACTION.md` и PLAN приведены; код 0.10.9 уже в этом формате |

Второй отчёт — новая сессия (Sonnet 5) около 13:35; она читала `F:\-7-` до слияния этого коммита:

| Замечание | Что сделано |
|---|---|
| README 0.3.0, «5 хуков» в DEPLOY, «hooks generation» в шаге 2 порядка старта | исправлено этим коммитом, см. выше; сессия видела дерево до слияния |
| `docs/GOAL.md` держит multi-session на 0.10.9 | переведён на нумерацию релизного поезда: ядро — 0.10.10, hardening — 0.10.11, GPT Chat + RDC — 0.10.12; исправлена склейка пунктов 4 и 5 |
| дерево не каноническое: изменён PLAN, есть feedback от 13:22 | это штатные записи `feedback` соседних сессий — включены в коммит; критерий канонического дерева в PLAN и PRODUCT_INSTRUCTION уточнён: незакоммиченные записи штатного `feedback` допустимы |
| P1-кандидат `FB-20260919T052235Z-83ec8ec3` (планировщик Codex, командная строка) | в каноническом PLAN; брать ли его в план работ — решение ЛПР |
| подсказка значка «ответ drift не разобран» у `srvlm01-airenv002-storage-order-0109` | воспроизведено: `drift -json` на негодном плане отвечает текстом — кандидат `FB-20260919T053702Z-44fa81f7`; негодный план того продукта — забота его владельцев |
| «в режиме ход заканчивается отчётом и строками "Решил / Нужно от тебя"» | по решению ЛПР (вариант А) — строки Ядра; `INTERACTION.md` приведён |

## 6. Независимое ревью Codex

Read-only, `gpt-5.6-sol`, effort medium — модель другого вендора, чем исполнитель.

Раунд 1 — `VERDICT=DRIFT`:
- проверка канонического дерева «`HEAD..origin/main` = 0» пропускала ветку, которая опережает `origin/main` или расходится с ним. Заменена на одну строку `git status -sb -- air-worker` = `## main...origin/main` — в PLAN и PRODUCT_INSTRUCTION;
- формулировка «Worktree убраны» спорила со списком оставленных — уточнена;
- четыре находки «проверить не могу»: песочница Codex не видит `E:\`, `F:\-0-` и само дерево `F:\-7-`. Для второго раунда внешние файлы и выводы команд собраны снимком.

Раунд 2 — `VERDICT=DRIFT`, по другим местам:
- `DEPLOY.md` называл клон обязательным и дважды по-разному говорил о бэкапе профиля — клон помечен необязательным, про профиль оставлено одно правило договора томов: в бэкап не входит;
- шаг 2 порядка старта обещал SHA значка и hooks generation, которых `install -status` не печатает, — сужен до того, что команды показывают, SHA и хуки проверяются названными командами;
- статус `closed` в строке PLAN и `candidate` в evidence-файле — записано правило: статус ведётся в PLAN, evidence неизменяем;
- проза про «перенос шага 38» спорила с таблицей, где шаг 38 закрыт, — приведена к таблице; порядок работ 0.10.10 по существу не менялся;
- утверждения receipt о побайтовой сверке, 285 соседних файлах, SHA и `Access is denied` не были представлены в снимке — выводы добавлены в снимок третьего раунда.

Раунд 3 — `VERDICT=DRIFT`:
- утверждения о значке были шире доказательств: повышение прав и «тот же» SHA — формулировки сужены до логов, SHA образа PID 12556 добавлен в снимок;
- дерево `F:\-7-` оказалось не каноническим: в нём появились записи `feedback` соседних сессий — они включены в коммит, а критерий канонического дерева уточнён;
- шаг 5 порядка старта требовал решений исполнителя (как переносить пачку, какие файлы `aw-step38` брать) — теперь старт заканчивается указанием следующей работы и её владельца.

Раунд 4 — `VERDICT=DRIFT`, две находки:
- гейты 71а, 61а, 69а, 65а, 56 и таблица backlog-кластеров держали нумерацию до перестановки релизного поезда (ядро 0.10.9 … semantic 0.10.12, архитектура 0.11.0), а поезд того же плана и `GOAL.md` — 0.10.10 … 0.10.13 и 0.12.0. Расхождение было в main и до канонизации. Номера сдвинуты построчно: каждая замена привязана к своей строке, сверка гейтов с поездом скриптом — 5 из 5;
- файл-доказательство для `drift -json` показывал `exit=0`: код подменял обрывающий конвейер (`Select-Object -First`). Повторный замер без конвейера — код 2 трижды подряд; утверждение кандидата `44fa81f7` верно, файл-доказательство переписан.

Обе находки механические и закрыты проверкой скриптом; пятый раунд модели не запускался.

## 7. Восстановление

- пачка 41/46: `git -C F:\-7- worktree add <путь> wip/air-worker-0.10.10-batch-41-46`;
- убранный worktree: `git -C F:\-7- worktree add <путь> refs/archive/air-worker-worktrees-20260919/<имя>`;
- удалённая ветка GitHub: `git -C F:\-7- push origin <sha>:refs/heads/<имя>`.
