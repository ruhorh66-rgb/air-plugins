# PLAN — air-worker: механизм в бинарнике, скил — описание

Постановка: `AIR_OS/04_AI_OPERATIONS/CHANGE_PACKAGES/AIR-CHG-2026-000092_AIR_WORKER_BINARY_ARCHITECTURE_OBJECTIVE.md`
Прежняя постановка: `AIR-CHG-2026-000091_AIR_WORKER_GUARANTEED_RESULT_OBJECTIVE.md` — шаги 1–25.
Формат: блок целей, критерии и таблица шагов со ступенями. **План правится руками.**

**Все указания ЛПР по продукту записываются в этот план в том же рабочем цикле.** ЛПР 14.09.2026: «Все мои указания должны попадать в план разработки продукта».

**Установка и обновление — только штатным путём из GitHub.** ЛПР 14.09.2026: «Только один путь верный — установка из GitHub… все другие попытки обновления неприемлемы… только штатный механизм обновления по аналогии с AIR Kill Switch, либо переустановка бинарника напрямую с правами — чтобы каждая сессия не правила код и не заливала то, чего ей хочется». Держится кодом, шаг 66.

**Архитектура, повторно утверждённая ЛПР 14.09.2026:** **Manifest подключает. Adapter доставляет. Binary решает. Models работают. Skill объясняет.**
- manifest — только регистрация плагина и его возможностей у хоста;
- adapter — только доставка события/задания между хостом и бинарником: hooks у Claude/Codex, handoff у GPT Chat + RDC;
- binary — единственный control plane: состояние, guards, гейты ЛПР, planner/executor orchestration, factual judge, semantic judge, engine, loop, report, install/update policy;
- models — только роли планировщика, исполнителя и semantic judge по правилам бинарника;
- skill — необязательный discovery/help-слой: кратко объясняет назначение и команды; никаких правил, грамматик, текстов отказов и управляющей логики.

**Инвариант skill:** отсутствие или незагрузка `SKILL.md` не меняет ни одного решения Air Worker. Проверка ChatGPT 14.09.2026: копия плагина без skill прошла `claude plugin validate`, код 0. Удалять skill ради удаления не требуется; цель — сделать его необязательным и коротким.

**Инвариант adapter:** adapter не интерпретирует состояние и не дублирует политику. Если host умеет напрямую вызвать `air-worker.exe hook <event>` с stdin/exit code, `.cmd`-обёртка не нужна; иначе допустима одна тонкая transport-обёртка без логики.

Границы протокола хуков Claude Code, сверенные Anthropic 15.09.2026, — они определяют, где обёртка всё же нужна:
- **Блокирует только код 2.** Другой ненулевой код и незапустившаяся команда — неблокирующая ошибка. При прямом вызове пропавший или не стартовавший бинарник даёт fail-open, поэтому control-событиям нужна одна transport-обёртка, превращающая «ответа нет» в отказ.
- **Паника Go завершает процесс кодом 2, то есть «блоком».** Бинарник в режиме `hook` перехватывает панику и отвечает по классу события. Иначе упавший бинарник на `Stop` не даёт сессии закончить ход.

**Политика отказа hooks/adapters:** до явного включения Air Worker для сессии/продукта — no-op; telemetry/lifecycle при внутренней ошибке — fail-open + trace; control/security event при активном Air Worker — fail-closed с названной причиной. Неизвестное информационное событие — fail-open + trace.

`Stop` и события субагентов — lifecycle. Решение стража хода действует, пока бинарник отвечает; при его отказе — пропуск со следом, иначе fail-closed на `Stop` не дал бы сессии закончить ход ни разу. Fail-closed — только для событий до действия (`PreToolUse`).

Эта политика реализуется бинарником, не hook-скриптом.

ЛПР: «Мы с тобой определились, что это только описание, а всё остальное, вся остальная работа уходит в механизм. А наш механизм — это бинарник, а хуки загоняют только работу в этот бинарник.» Повторная архитектурная сверка ChatGPT 14.09.2026 согласована ЛПР: умным слоем остаётся только binary.

**План пересобран 14.09.2026 планировщиком** по указанию ЛПР («Пересобирай свой план, запусти его ещё раз через планировщик»). Прогон: `air-worker plan`, opus, один вызов, $1.2276, 9 ходов, предложено 19 шагов. Ведущая сверила разбивку и поправила четыре места:
- три шага на ступени `script` без команды переведены на модель: петля не исполнит `script` без команды;
- страж работы переведён с `opus` на `sonnet`: образец готов;
- сжатие скила переведено с `haiku` на `sonnet`: решать, что оставить, — суждение;
- граница судьи сведена к `run-config.json` и своему судье, как доложено ЛПР. Скрипты проверок — тесты продукта, их закрытие подтверждает К19.

Колонка «Судья» исполняемого шага начинается со ссылок на критерии. Закрытый шаг — зачёркнутый номер `~~N~~`. Шаг-гейт — прочерк в колонке ступени.

**Разделение по вендорам — решение ЛПР 14.09.2026.** air-worker кросс-платформенный:
- сторона GPT — ChatGPT: Codex-хост, исполнитель и планировщик Codex;
- сторона Anthropic — Claude: Claude-хост и общий код бинарника.

Шаги стороны GPT помечены в колонке «Судья» словами «сторона GPT», остальные — сторона Anthropic.

**Модель стоит у исполнителя, у планировщика и у семантического судьи**; стражи и двигатель — код. Судья двухконтурный:
- фактический судья — код: работает ли то, что доказывается машиной;
- семантический судья — модель другой природы, чем исполнитель: то ли сделано и туда ли идём.

Решение — пересечение двух вердиктов: модель не спорит с машиной, но может остановить. ЛПР 14.09.2026 о семантическом судье: «мне кажется, что это есть там суть».

**Планировщик — дорогая модель того же вендора, что исполнители.** Независимость даёт семантический судья другой природы. ЛПР 14.09.2026: «планировщик тоже должен быть моделью того же вендора, но просто дорогой и правильно планировать работу. Тогда у нас есть, кому проверять». Прежнее правило «планировщик другой природы» отменено.

**Semantic Plan Review — утверждено ЛПР 14.09.2026.** Это не новая роль, а режим существующего semantic judge: `subject=plan`. После того как планировщик предложил план и человек принял/отредактировал финальный `PLAN.md`, но до первой итерации исполнения, модель другого вендора делает короткое read-only ревью именно этого плана. Контракт: `PASS | COMMENTS | DRIFT`; до трёх `MISSING`, до трёх `REDUNDANT`, до трёх `ORDER_RISK`, одна строка `NEXT`; вывод до 500 токенов. Reviewer ничего не правит. `PASS` — запуск разрешён; `COMMENTS` — замечания показаны, запуск разрешён; `DRIFT` — запуск блокируется до изменения плана и нового review либо явного решения ЛПР. Результат хранит `plan_sha256`, reviewer vendor/model, verdict и timestamp; изменение `PLAN.md` автоматически делает review устаревшим.

## Состояние и порядок старта

По стандарту `AIR_PRODUCT_DEVELOPMENT_PLAN`: этот план — единственное место для хвостов, дефектов и решений. В переписке они не остаются.

- **Выпущено:** 0.10.4 — release commit `49827f4`, tag `air-worker--v0.10.4`, origin/main и tag запушены 15.09.2026. Release gate: targeted К56–К58, `go test ./...`, `go vet ./...`, `git diff --check`, plugin-check, goals, version — PASS. Claude plugin обновлён штатно до 0.10.4; Codex marketplace snapshot обновлён до 0.10.4; SHA-256 source/Claude/Codex binary совпадает: `A08C5250BFADFFEA6D1F0AF88F3C016DEECCA2D3FECCC2CB9408751384B84DBC`.
- **Критический путь после 0.10.4:** 39 → 83 → 73 → 71 → 38 → 41 → 46 → 74. Сначала identity/isolation нескольких пользователей и сессий, затем durable jobs и PlanState, после этого protection hooks/guard и безопасный single-step. 79/72/75/40/49/61 — следующий hardening-слой и не блокируют первый защищённый multi-session kernel.
- **AirVera / live runtime:** AirVera — один consumer workload и источник эксплуатационных дефектов, не архитектурный владелец AirWorker/AirBot. Уже запущенный AirVera job не прерывать и не переустанавливать поверх процесса; новые сессии используют новый tagged plugin/runtime. Целевая архитектура обязана допускать несколько version-pinned jobs одновременно и не иметь глобального singleton-lock на рабочие сессии.
- **Anthropic-наработки 38/40 не выбрасываются.** `aw-step38` — подготовленный материал protection-layer. Шаг 38 переносится в 0.10.5 только после появления session/principal scope (39/83), чтобы protection hooks сразу были multi-session-safe, а не закрепляли глобальное состояние.
- **Рабочие копии:**
  - `F:\-7-\_worktrees\aw-step63` — историческая; `aw-step66` — историческая после commit `58746c0`, перенесённого в main как `a950b73`; `aw-step38` — подготовленная копия protection-layer, переносится только после session/principal scope 39/83;
  - почему копии: основное дерево одновременно правит сторона GPT, а два субагента в одном Go-пакете ломали бы тесты друг другу;
  - дифф в основное дерево переносит ведущая. Шаг 62 перенесён 14.09.2026: `go vet` и `go test` зелёные;
  - первая пара субагентов не пережила переполнение контекста ведущей и работы не оставила.
- **Текущий исполнитель:** ChatGPT. После выпуска 0.10.4 первый work-step — 39, затем 83 → 73 → 71 → 38 → 41 → 74. Это минимальный путь к защищённой multi-session/multi-user эксплуатации.
- **Тупик fresh-plan до шага 64 закрыт в 0.10.2.** Текущий измеренный bottleneck — factual judge на собственном PLAN: `plan-only` 15.09.2026 занял 92.83 с из-за отдельного `go test -list` на множество pending selectors. Это не меняет вердикт, но тормозит каждый цикл; исправление включено в 72 / К51 как единый selector inventory на judge-run.
- **Приведение машин к штатному пути, указание ЛПР 14.09.2026:**
  - ноутбук — AIR-ENV-002: маркетплейс air-plugins перевести с каталога `F:\-8-\air-plugins` на GitHub и переставить 0.10.0 штатно; бинарник, извлечённый из тега, установкой не считается. Маркетплейс переводится командами `claude plugin marketplace remove air-plugins` и `claude plugin marketplace add ruhorh66-rgb/air-plugins`: правка `settings.json` источник не меняет (шаг 66). 14.09.2026 сделано:
    - `air-worker version` — 0.10.0, SHA-256 совпал с эталоном тега, клон `F:\-8-\air-plugins` не тронут;
    - команд понадобилось четыре, а не две: `marketplace remove` снял и регистрацию плагина, поэтому нужен `plugin install`; плагин встал выключенным, поэтому нужен `plugin enable`;
    - пользовательская копия скила `~/.claude/skills/air-woody` версии 0.9.4 заслоняла плагинный скил — снята с резервной копией (шаг 66);
  - SRVLM01 — Claude: GitHub marketplace/plugin cache штатно обновлён до 0.10.4; cache binary SHA совпадает с release. Уже запущенные jobs не прерываются; текущая host-session требует restart только чтобы загрузить новую hooks generation.
  - SRVLM01 — Codex: marketplace `air-plugins` штатно обновлён до tag 0.10.4; `air-worker@air-plugins` installed/enabled 0.10.4, SHA binary совпадает с release.
- **Владение работой:** текущая ChatGPT-сессия ведёт semantic hotfix 81–82, затем operational kernel. Anthropic-наработки сохраняются изолированно и переносятся только в своей очереди после соответствующего release gate.
- **Координация:** перед переносом из старого worktree сверяется `git diff`; незавершённый код следующего шага не попадает в промежуточный релиз.
- **Заблокировано:** нет.
- **Отложено:** раздел «Что сознательно не в плане».
- **Откат выпуска:** бинарник — `air-worker install` из кэша прежней версии плагина (`…\plugins\cache\air-plugins\air-worker\<версия>\bin\air-worker.exe install`), с шага 66 — только с повышенными правами, руками ЛПР; плагин — возврат коммита в маркетплейсе и `claude plugin update`.

**Релизный поезд, решение ЛПР 14.09.2026:** рабочие сессии тестируют Air Worker параллельно разработке; самостоятельный зелёный слой выпускается сразу и не ждёт крупной вехи. Каждый выпуск включает только закрытые шаги и проходит `go test ./...`, `go vet ./...`, `git diff --check`, проверку plugin manifests и живой `air-worker version`.

- **0.10.1 — operational hotfix:** 58 + 59 + 62 + 63. Codex write/env, planner того же вендора, script-step closure, обязательный годный PLAN на основных входах.
- **0.10.2 — factual-plan + semantic step judge:** 60 + 64 + 65. Строгие меры/селекторы и промежуточный профиль ChatGPT orchestration → Anthropic ladder → Codex read-only semantic judge.
- **0.10.3 — safe distribution + chat orchestration:** 66 + 67 + 80. GitHub-source для Claude/Codex и первый штатный binary-owned orchestration path: ChatGPT session запускает Anthropic agents через AirWorker без ручного поиска host-синтаксиса.
- **0.10.4 — semantic reliability hotfix:** 81 + 82. Codex semantic packet не идёт через Windows argv; step review получает step-scoped factual evidence; тот же read-only semantic judge доступен standalone для ChatGPT/RDC и других внешних executors.
- **0.10.5 — multi-session protection kernel:** 39 + 83 + 73 + 71 + 38 + 41 + 46 + 74. Явные `principal/session`, изоляция state/locks/jobs, durable receipts, единый PlanState, binary protection hook/guard и безопасный single-step. Это первая версия, которую можно считать архитектурно пригодной для нескольких одновременных сессий одного AirBot/AirWorker.
- **0.10.6 — operational hardening + orchestration observability:** 79 + 72 + 75 + 40 + 49 + 61. Distribution selfcheck, validate/selector batching, дешёвая observability, учёт субагентов, execution metrics/topology и единые роли поверх уже изолированного multi-session kernel.
- **0.10.7 — GPT Chat + RDC:** 69. First-class external executor поверх job receipt: один task packet на один инструментальный прогон GPT-5.6 Sol/high, целевой бюджет ≤20 минут.
- **0.10.8 — semantic control completion:** 68 + полная кросс-вендорная симметрия. Semantic Plan Review после стабилизации operational kernel и host adapters.
- **0.11.0 — binary architecture cleanup:** 42–55 + 70 + 76–78. Thin adapters, необязательный короткий skill, штатные операции PlanState, acceptance types, bootstrap/recovery guard и устранение дублирующей логики.

- **Старт новой сессии:**
  1. прочитать этот план;
  2. `git -C F:\-7- log -3` — лёг ли выпуск;
  3. `air-worker goals -product F:\-7-\air-worker`;
  4. `air-worker report -product F:\-7-\air-worker`;
  5. взять первый открытый шаг своей стороны.

## История — этапы до 0.10

| № | Шаг | Ступень | Судья |
|---|-----|---------|-------|
| ~~1~~ | Судья Вуди заведён в продукте: `run-config.json` с проверками, реестр фактов под git | `script` | исполнитель: ведущая |
| ~~2~~ | Самотест `air-worker` объявлен проверкой судьи и проходит через него | `script` | исполнитель: ведущая |
| ~~3~~ | Лестница проверяется прогоном на всех шести ступенях, а не только на нулевой | `sonnet` | исполнитель: субагент |
| ~~4~~ | Цель как файл: описание цели версионируемым файлом, исполнитель читает его первым действием | `sonnet` | исполнитель: субагент |
| ~~5~~ | Планировщик: разбивка цели на шаги с назначением ступеней одним дорогим вызовом | `opus` | исполнитель: ведущая |
| ~~6~~ | Двигатель: один запуск крутит до вердикта либо до потолка, без задачи планировщика ОС | `sonnet` | исполнитель: субагент |
| ~~7~~ | Застой называется вслух: N итераций без сдвига вердикта снимают цель с вращения | `script` | исполнитель: ведущая |
| ~~8~~ | Воспроизводимость: тот же набор файлов в новой сессии даёт тот же путь к вердикту | `script` | исполнитель: ведущая |
| ~~9~~ | Выпуск 0.4.0: тег, CHANGELOG, запись в маркетплейс | `script` | исполнитель: ведущая, по решению ЛПР 12.09 |
| ~~10~~ | Решение о переходе на бинарник: принято ЛПР 13.09.2026 — переходим | — | гейт: ЛПР, пройден |
| ~~12~~ | Бинарник: судья и двигатель одним файлом, равенство со скриптом проверяется прогоном | `script` | исполнитель: ведущая |
| ~~13~~ | Петля перенесена в бинарник, договор цели снят с одной машины | `script` | исполнитель: ведущая |
| ~~14~~ | Планировщик перенесён в бинарник, разбивка совпала со скриптом на одном ответе | `script` | исполнитель: ведущая |
| ~~15~~ | Поиск инструмента — одной общей функцией: правило «по ответу, а не по имени» стало кодом | `script` | исполнитель: ведущая |
| ~~16~~ | Общие функции проверяются на той оболочке, где живут: BOM счётом, резолвер прогоном под 5.1 и pwsh | `script` | исполнитель: ведущая |
| ~~17~~ | Плагин приведён к каноническому виду: хуки объявлены плагином переносимым путём, ни одного адреса машины | `script` | исполнитель: ведущая |
| ~~18~~ | Выбор исполнителя виден сухо: подкоманда tool печатает путь и правило, ничего не запуская | `script` | исполнитель: ведущая |
| ~~19~~ | Взаимодействие с ЛЛМ названо границей, а не сведено переносом: docs/BOUNDARY.md | `opus` | исполнитель: ведущая |
| ~~20~~ | Точки входа объявлены: скил для работы, бинарник для управления; обе проверяются | `opus` | исполнитель: ведущая |
| ~~21~~ | Самодостаточность решена: Python остаётся, поглощать соседний продукт не будем | `opus` | исполнитель: ведущая |
| ~~22~~ | Выпуск 0.5.0: бинарник, канонический плагин, граница, двенадцать проверок | `script` | исполнитель: ведущая |
| ~~23~~ | Первый НАСТОЯЩИЙ прогон петли: вход — практическая задача от ЛПР, механизм её не выдумывает | — | гейт: ЛПР · закрыт ChatGPT 14.09.2026 в рамках задачи ЛПР на выпуск 0.10.0 |
| ~~24~~ | Первый настоящий прогон состоялся: две итерации на ASW, $7.33, пять дефектов механизма найдено | `script` | исполнитель: ведущая |
| ~~25~~ | Исполнителю передаётся список инструментов одной строкой — как в проверенной ветви | `script` | исполнитель: ведущая |
| ~~11~~ | Двигатель цели: ход, не уменьшивший расстояние до цели, продвижением не считается | `script` | исполнитель: ведущая |

## Цели — этап 0.10: работа только по плану с целями

Этап появился по замечанию ЛПР 14.09.2026, переданному AIR-ENV-002: **без целей план — бумажка, не видно, пришли к цели или нет.** Для закрытых шагов задним числом блок не пишется.

**Ц1.** air-worker не исполняет работу, про которую не объявлено, к какой цели она ведёт и чем это будет доказано; держится это механизмом, а не памятью сессии.
Основание: решение ЛПР 14.09.2026 — «без плана и без целей air-worker не работает».

| Критерий | Цель | Признак достижения | Чем меряется |
|---|---|---|---|
| К1 | Ц1 | план без блока целей петля не исполняет и называет причину | проверка `тесты бинарника` |
| К2 | Ц1 | исполняемый шаг без ссылки на критерий не исполняется | проверка `тесты бинарника` |
| К3 | Ц1 | критерий без проверки судьи и без факта реестра — «нечем проверить», а не молчание | проверка `тесты бинарника` |
| К4 | Ц1 | планировщик без постановки отказывает и сам начинает разбивку с блока целей | проверка `тесты бинарника` |
| К6 | Ц1 | образец плана описывает блок целей и разбирается тем же кодом | проверка `тесты бинарника` |
| К7 | Ц1 | `install -status` называет причину отсутствия доказательства значка, а не догадку | проверка `тесты бинарника` |
| К10 | Ц1 | совет двигателя и петли, когда судья не видит работы плана, называет гейт ЛПР, а не правку судьи | проверка `тесты бинарника` |
| К32 | Ц1 | план как файл обязателен на каждом входе бинарника в работу: петля, судья, двигатель и отчёт, объявление продукта. Путь плана один — `plan` из `run-config.json`, по умолчанию PLAN.md. Нет файла плана с годным блоком целей — отказ кодом 2 с путём, а не работа без плана | проверка `тесты бинарника` |

Критерии К5, К8 и К9 первой редакции сняты 14.09.2026 вместе с их реализацией на PowerShell. Их содержание перенесено в бинарник: К16, К12, К20.

| № | Шаг | Ступень | Судья |
|---|-----|---------|-------|
| ~~26~~ | Образец плана `PLAN.example.md` описывает блок целей и разбирается тем же кодом, что план; SKILL.md — шаг 53 | `sonnet` | К6: TestОбразецПланаСодержитБлокЦелей пройден 14.09.2026 |
| ~~27~~ | Петля отказывает без блока целей и на шагах без критерия — код 2, причина и номера шагов | `sonnet` | К1, К2: TestПетляНеИсполняетПланБезЦелей и TestШагБезКритерияНазванПоНомеру пройдены 14.09.2026 |
| ~~28~~ | Судья: критерий без проверки и без факта реестра даёт «нечем проверить», код 2 | `sonnet` | К3: TestКритерийБезПроверкиНечемПроверить пройден 14.09.2026 |
| ~~29~~ | Планировщик: без постановки отказ; разбивка начинается с блока целей | `sonnet` | К4: TestПланировщикБезПостановкиОтказывает пройден 14.09.2026 |
| ~~30~~ | Блок целей дописан в планы air-worker и ASW — иначе новая петля их не возьмёт | `opus` | К1, К2: `air-worker goals` = 0 на обоих планах 14.09.2026; в ASW блок дописала claude-bd |
| ~~31~~ | Решение ЛПР 14.09.2026: гейт записывает сессия — дословно надиктованные слова ЛПР, со временем и сроком. «Я практически всегда надиктовываю… пока сессия записывает мои слова и фиксирует в гейт. Когда начнутся провалы, я начну это подсвечивать» | — | гейт: ЛПР, пройден · К12 |
| ~~33~~ | `install -status`: вместо «старая версия трея?» — путь файла доказательства и что с ним не так | `sonnet` | К7: TestДоказательствоЗначкаНазываетПричину пройден 14.09.2026 |
| ~~37~~ | Двигатель и петля не советуют сессии «расширить судью»: судья, не видящий работы плана, — гейт ЛПР в плане. Прежний совет WORKER-DRIFT-04 «Расширить судью на эти шаги» привёл сессию Vera в код судьи | `sonnet` | К10: TestСудьяДоволенАПланГоворитИное сверяет текст совета, пройден 14.09.2026 |
| ~~37а~~ | Выпуск 0.10.0 — поручен ЛПР ChatGPT 14.09.2026; сигнал Anthropic «судья зелёный» получен, выпуск выполнен ChatGPT | — | гейт: ЛПР · сторона GPT · 14.09.2026 |
| ~~62~~ | Шаг `script` с кодом 0 закрывается петлёй в плане, ступень не поднимается. Нашла AIR-ENV-002 14.09.2026: после «НОРМА» петля подняла ступень до haiku, и haiku на пройденном шаге за $0.95 сочинил документ с выдуманными решением ЛПР и архитектурой и доложил «готово», ничего не запустив | `sonnet` | К31: тест петли на успешный и неуспешный шаг `script` |
| ~~63~~ | План как файл обязателен на всех входах бинарника. В 0.10.0 его требует только петля: судья без плана выносит «цель достигнута», двигатель и отчёт считают расстояние без плана и берут прибитый `PLAN.md` мимо `run-config.json`, продукт объявляется без плана (`mode.ps1 -Product` проверяет только заготовку). Одна проверка — та же, что у `air-worker goals`, — на всех входах; объявление продукта отказывает по её коду. Правка файлов сессией без плана — шаг 42 | `sonnet` | К32: тест входов на продукте без плана · исполнитель: ChatGPT |
| ~~63а~~ | Выпуск 0.10.1: operational hotfix — шаги 58, 59, 62, 63; опубликован commit `2222f83`, tag `air-worker--v0.10.1`, release gate зелёный | — | гейт: ЛПР · выполнен ChatGPT 14.09.2026 |
| ~~64~~ | Фактический судья видит работу плана через критерии. Нашла Vera 14.09.2026: на свежем плане ВЕРЫ (12 шагов от планировщика) судья зелёный — проверок 3, фактов 5 из 5, — а петля до первой итерации останавливается «СУДЬЯ ДОВОЛЕН, А ПЛАН ГОВОРИТ ИНОЕ»: тесты шагов 3–11 ещё не написаны, а написать их и должна петля. Свежий продукт так не стартует никогда. Мера критерия — проверка судьи, при нужде с селектором теста: ``проверка `имя` · тест `селектор` ``, либо ``факт `id` ``. Селектор передаётся проверке по шаблону `select` из её описания в run-config.json (go — `-run ^{}$`, pytest — `{}`); шаблон вносится по гейту ЛПР на судью. Прозу в мере `goals` отклоняет. Ненаписанный или упавший тест — критерий не подтверждён и входит в остаток: петля получает работу. Зачёркнутый шаг засчитывается движением, только если подтверждены его критерии (прежний шаг 48). Это новое правило расстояния — историю до него не сравнивать. ЛПР 14.09.2026: судья сравнивал работу по плану «с какими-то суждениями непонятными» — устранить. «Признак достижения» остаётся прозой — вход семантического судьи (шаг 65). Планировщик пишет меры в этой грамматике; образец плана и этот план переводятся на неё, claude-bd и Vera получают правило | `sonnet` | К33, К19: тесты судьи на мере с селектором, на ненаписанном тесте, на прозе и на неподтверждённом закрытии; петля берёт свежий план при зелёном судье |
| ~~64а~~ | Выпуск 0.10.2: factual-plan + semantic step judge. Промежуточный профиль ЛПР 15.09.2026: оркестратор — чатовая сессия ChatGPT; исполнитель — лестница Anthropic; semantic judge — Codex read-only. Обратный Codex→Claude маршрут остаётся заготовкой будущей кросс-вендорности и в активном профиле не используется. Шаги 60, 64, 65 | — | гейт: ЛПР |
| ~~66~~ | Установка и обновление только штатным путём из GitHub — решение ЛПР 14.09.2026. `air-worker install` ставит только бинарник из кэша плагина маркетплейса air-plugins с источником GitHub; бинарник из рабочего дерева, из каталога-маркетплейса или извлечённый из тега отклоняется кодом 2 с причиной. Источник маркетплейса читается из действующей записи `plugins\known_marketplaces.json` в каталоге конфигурации Claude; `extraKnownMarketplaces` в `settings.json` — заготовка, её правка источник не меняет. Нашла AIR-ENV-002 14.09.2026: после правки `settings.json` маркетплейс остался каталогом, `claude plugin update` ответил «уже последняя версия (0.9.4)» кодом 0, и установка из кэша молча откатила машину с 0.10.0 на 0.9.4. Понижение версии `install` отклоняет кодом 2 и называет обе версии. Прямая переустановка бинарника, в том числе откат, — только с повышенными правами, то есть руками ЛПР. Обновление — штатным механизмом по образцу обновления AIR Kill Switch. `install -status` называет источник установленного бинарника и источник маркетплейса и печатает нарушение. Нарушение — и пользовательская копия скила, заслоняющая плагинный скил: у AIR-ENV-002 при бинарнике 0.10.0 грузилась `~/.claude/skills/air-woody` версии 0.9.4. Переход машины с каталога на GitHub — четыре команды: `claude plugin marketplace remove air-plugins`, `claude plugin marketplace add ruhorh66-rgb/air-plugins`, `claude plugin install air-worker@air-plugins`, `claude plugin enable air-worker@air-plugins`. `remove` снимает и регистрацию плагина, а `install` ставит плагин выключенным. Недостающую команду печатает `install -status`. Страж команд (через бинарник) отклоняет подмену маркетплейса каталогом и установку в обход. Там же: отказ установщика не отдаёт код 0 — у AIR-ENV-002 ветка «рядом нет air-worker-tray.exe» вернула 0 при `return 2` в коде; код до `os.Exit` доходил и в 0.10.0: ноль у AIR-ENV-002 дала обёртка вызова снаружи Go. Теперь у процесса одна точка выхода `run() int`, её проверяет тест прогоном самого процесса. После установки версия по имени и SHA-256 бинарника сверяются с поставленными; расхождение — код 2, а не строка при коде 0 | `opus` | К37, К36: тесты установщика на источниках, понижении версии, правах и коде возврата · исполнитель: Anthropic · закрыт 15.09.2026; commit `a950b73` в main, `go test`/`go vet` зелёные; live `install -status` после миграции marketplace на GitHub — «нарушений источника нет» |
| ~~67~~ | Codex source parity: `install`/`install -status` понимают `%CODEX_HOME%\plugins\cache\air-plugins\air-worker\<версия>` и GitHub marketplace из Codex config наравне с Claude; canonical GitHub source и anti-downgrade policy едины | `sonnet` | К36, К37: unit/vet PASS; live SRVLM01 15.09.2026 — Claude+Codex `github ruhorh66-rgb/air-plugins`, оба plugin 0.10.2, SHA совпадает, «нарушений источника нет» · исполнитель: ChatGPT |
| ~~80~~ | Chat orchestration bootstrap в binary. По команде/override AirWorker сам включает orchestration для конкретного прогона, добавляет native `Agent` tool без временной правки canonical run-config, требует фактический запуск `N` субагентов и наблюдает его через Claude stream-json/forwarded subagent events. Prompt-only «раздай работу» не считается оркестрацией. В receipt/steps пишутся requested/started/completed agent counts и причины blocked/unavailable. Базовый паттерн взят из AirLegal W20: per-pass orchestration + native Agent; усиление — доказательство реальных Agent calls. ChatGPT остаётся orchestrator, Anthropic — executor lane, Codex — semantic judge | `sonnet` | К55: `TestCriterion55AgentEvidence`; unit/vet PASS; live smoke SRVLM01 15.09.2026 — requested/started/completed = 2/2/2, factual PASS, Codex semantic PASS · исполнитель: ChatGPT |
| ~~66а~~ | Выпуск 0.10.3: safe distribution + chat orchestration — шаги 66, 67 и 80; ЛПР 15.09.2026 поручил после настройки orchestration выпустить новый релиз. Live smoke: Agent lifecycle 2/2/2, factual PASS, Codex semantic PASS; release gate unit/vet/plugin/goals/version зелёный | — | гейт: ЛПР · решение дано 15.09.2026 |
| ~~81~~ | Windows-safe Semantic Judge transport. Codex semantic packet передаётся через stdin (либо управляемый temp-file fallback), но никогда целиком через argv/`codex.cmd`; длинный packet >32 KiB проходит на Windows. Args содержат только короткие flags/schema/path, read-only sandbox сохраняется | `sonnet` | К56: `TestCriterion56Pending` · исполнитель: ChatGPT |
| ~~82~~ | Step-scoped Semantic Judge + standalone entrypoint. Semantic review шага получает состояния только критериев текущего шага и отдельный step factual verdict; будущие критерии продукта не превращают выполненный шаг в factual FAIL. `air-worker semantic|review -product ... -step ...` переиспользует тот же packet/schema/history без запуска executor loop | `sonnet` | К57, К58: `TestCriterion57Pending`, `TestCriterion58Pending` · исполнитель: ChatGPT |
| ~~81а~~ | Выпуск 0.10.4: semantic reliability hotfix — шаги 81–82; live Windows smoke: длинный Codex packet через stdin, step-scoped PASS при открытых будущих критериях, standalone read-only review | — | релизный поезд ЛПР 14.09.2026 + прямое поручение 15.09.2026; preflight PASS; commit `49827f4`, tag `air-worker--v0.10.4`; Claude/Codex 0.10.4, SHA совпадает |
| ~~39~~ | Host-neutral session identity/state в binary: `air-worker session declare -product ... -principal ... -session-key ...`; binary хранит режим, продукт, гейт/возврат и host/profile state только в namespace этой identity и никогда не угадывает identity из host-specific env. Здесь доказывается только host-neutral binary contract; доставка identity host-adapters и снятие legacy `mode.ps1` — шаг 46 после binary hooks/guard. Два Codex NOT_PROVEN 15.09.2026 14:29/14:39 выявили прежнюю ошибку порядка, а не дефект binary identity | `sonnet` | К44: `TestCriterion44Pending`; после корректировки scope требуется standalone Codex semantic PASS · исполнитель: Claude · factual К44 PASS · Codex semantic PASS 15.09.2026 14:48:56, step=yes, drift=none |
| 83 | Multi-session / multi-user isolation core. Все mutable state, locks, job ownership и session-level guard state получают namespace `(product, principal, session)`; job добавляет `job_id`. Две независимые сессии одного продукта реально стартуют одновременно и не блокируют/закрывают/перезапускают работу друг друга; попытка cross-scope mutation fail-closed с причиной. Machine-wide install/tray/version metadata остаётся общим только как read-only host state | `sonnet` | К62: `TestCriterion62Pending` · исполнитель: ChatGPT |
| 73 | Durable long-running job receipt для planner/judge/executor: receipt создаётся ДО запуска и содержит `job_id`/PID/started_at/status/output_path/product/principal/session/step/operation; transport/RDC timeout оставляет `RUNNING`, stdout читается по receipt; duplicate start запрещён внутри owned scope, но соседние session/job продолжают работать параллельно | `sonnet` | К42: `TestCriterion42Pending` |
| 71 | Единый PlanState и canonical PLAN: product PLAN один, а runtime view/gates/confirmed closures не смешивают session namespaces. `GATED` отделён от UNKNOWN/NOT_PROVEN; product-level governance меняется только разрешённой операцией, session-level state не может закрыть gate или work другой сессии. Legacy `min_facts` дедуплицирован | `sonnet` | К40, К59, К62: `TestCriterion40Pending`, `TestCriterion59Pending`, `TestCriterion62Pending` |
| 38 | Подкоманда `air-worker hook <событие>`: событие читается со stdin, решение полностью принимает binary. Политика отказа зависит от класса события: до активации Air Worker — no-op; telemetry/lifecycle — fail-open + trace; control/security при активном Air Worker — fail-closed с причиной; неизвестное информационное событие — fail-open + trace. Классификация событий живёт в одном месте бинарника. Перенесён из 0.11 в релизный поезд 0.10.x решением ЛПР; целевой выпуск 0.10.5. После переноса шага 66 сюда же подключается его страж обхода `classifyBypass` — это control-событие `PreToolUse`; prerequisite: 39/83 session isolation | `sonnet` | К11, К15: тест Go на каждый класс события, неизвестное событие и ошибку binary; паника в режиме `hook` даёт ответ по классу события, а не код 2 · исполнитель: Anthropic · готов в копии `aw-step38`, проверен ведущей 15.09.2026: 13 тестов, в том числе код выхода подпроцесса; `go vet` и `go test` зелёные; перенос — в выпуск 0.10.5 |
| 41 | Решение стража хода в бинарнике: сверка чисел со своим последним отчётом, оркестрация (К22), ожидание ЛПР по открытому гейту плана, вердикт ESCALATE двигателя останавливает ход сессии, как останавливает петлю. ЛПР 14.09.2026: страж «просто сверяет два числа» — сверка без действия по вердикту ухода в сторону не останавливает. При живом субагенте ведущей страж не требует перепрогона судьи на каждом ходу: дерево меняет исполнитель, как при идущей петле. 14.09.2026 страж 0.10.0 остановил ход ведущей, пока субагенты шагов 62 и 63 правили дерево. Продукт объявлен, а его план не годен — с 0.10.1 судья, двигатель и отчёт отвечают на нём кодом 2. Страж называет это в ходе, а не пропускает ход молча, как `turn-guard.ps1` при пустом замере. Нашла claude-bd 15.09.2026 на `srvlm01-mirror-backup-hardening`; scope: current product/principal/session | `sonnet` | К20, К22, К34: тест Go на каждое решение стража хода, в том числе на объявленный продукт с негодным планом |
| 46 | Session identity adapter migration: после готовых 38/41 hooks сводятся к transport-only path в binary. Claude/Codex adapters и generic external/GPT identity envelope передают explicit `principal` + `session key` без policy/state; direct host call используется где доступен, иначе одна тонкая wrapper. `mode.ps1` и shipped `.ps1` governance снимаются; исторические legacy-mode tests заменяются binary/transport tests. Полная GPT Chat+RDC execution semantics остаётся шагом 69 | `sonnet` | К12, К15, К63: `TestCriterion12Pending`, `TestCriterion15Pending`, `TestCriterion63IdentityAdapterPassThroughPending` |
| 74 | Безопасное исполнение шага: явные `script:action`/`script:probe` (или эквивалентный `close_on_exit_0`) и штатный `loop --once`/`--max-work-steps 1`; рабочие эксплуатационные сессии могут гарантированно отдать механизму ровно один work-step; adjacent session/jobs stay runnable | `sonnet` | К31, К43: `TestCriterion31ScriptActionProbePending`, `TestCriterion43Pending` |
| 71а | Выпуск 0.10.5: multi-session protection kernel — 39, 83, 73, 71, 38, 41, 46, 74; обязательный live acceptance: минимум две одновременные session identities на одном продукте, независимые jobs/state, cross-session mutation fail-closed, завершение одной сессии не останавливает вторую, legacy mode.ps1 отсутствует и adapters transport-only | — | гейт: релизный поезд ЛПР 14.09.2026; отдельный повторный гейт не нужен при зелёном acceptance |
| 79 | Distribution identity + installed selfcheck. Release-check запрещает изменённый plugin payload под прежней distribution identity; selfcheck/status показывают installed version + git/revision, marketplace snapshot, активные Claude/Codex config dirs и предупреждают о нескольких профилях. `air-worker selfcheck` работает из installed cache/live runtime без source checkout; same-version mismatch не маскируется `already latest` | `sonnet` | К52, К53: `TestCriterion52Pending`, `TestCriterion53Pending` · исполнитель: ChatGPT |
| 72 | `air-worker validate` + selector engine: PLAN grammar/columns/id, criterion ranges/иерархические ID, suspicious reuse одного критерия последовательными work-steps, tier→ladder→runner→tool, `script` command, dev-path dependency. Go/pytest inventory строится один раз; поддерживаются package-scope и явные selector cwd/env/tmp; outcomes различают package/test missing, test fail и infrastructure unavailable | `sonnet` | К41, К51, К60, К61, К26: `TestCriterion41Pending`, `TestCriterion51Pending`, `TestCriterion60Pending`, `TestCriterion61Pending`, `TestCriterion26Pending` |
| 75 | Дешёвая structured observability: `judge -json` с состоянием/selector/duration/reason/timing каждого критерия и checks/selectors; `report --cached`/`--no-judge` для навигации и `--informational`/`--no-fail` для цепочек команд без ложного abort на открытой цели. Текстовый report/judge используют ту же структуру данных | `sonnet` | К45, К54: `TestCriterion45Pending`, `TestCriterion54Pending` |
| 40 | Учёт субагентов в бинарнике: старты сводятся с остановками, остановка без старта названа числом, умерший на лимите или прерванный субагент снимается, а не висит живым. У claude-e6 14.09.2026 учёт показывал 5 живых при одном работающем | `sonnet` | К13: тест Go на три случая учёта и на субагента без остановки · исполнитель: Anthropic, в работе с 15.09.2026, копия `aw-step38` |
| 49 | Отчёт числом: ходы ведущей, запуски субагентов, итерации петли и расход; «двигателю нечего делать» и «двигатель не звали» — разные строки. Cached/full judge уже закрывает шаг 75; здесь только execution metrics поверх единого PlanState | `sonnet` | К21: `TestCriterion21Pending` |
| 61 | Имена ролей в бинарнике — одним местом: цель, factual judge, semantic judge, orchestrator, executor, engine, planner, guard. `air-worker report` печатает topology одной строкой (`orchestrator=ChatGPT / executor=Anthropic / semantic-judge=Codex / current-tier=...`) плюс фактический host/vendor/model. Слово «разбивщик» заменено на «планировщик» | `sonnet` | К30: тест имён ролей и topology |
| 61а | Выпуск 0.10.6: operational hardening + orchestration observability — 79, 72, 75, 40, 49, 61 поверх multi-session protection kernel 0.10.5 | — | гейт: релизный поезд ЛПР 14.09.2026 |
| 69 | GPT Chat + RDC adapter: binary формирует воспроизводимый task packet текущего шага (цель, критерии, корень, scope, ожидаемый результат, job_id и финальный factual judge). GPT Chat исполняет через RDC как first-class external executor; transport timeout = RUNNING и следующий прогон сначала poll-ит job. Фактические host/vendor/model/effort пишутся в measurement, AirWorker не запускает вторую модель. Packet ≤20 минут кодинга, жёсткое окно 20–26 минут; более крупный шаг заранее дробится | `sonnet` | К39, К42, К47: `TestCriterion39Pending`, `TestCriterion42Pending`, `TestCriterion47Pending` · исполнитель: ChatGPT |
| 69а | Выпуск 0.10.7: GPT Chat + RDC first-class external executor поверх operational kernel; рабочие сессии тестируют handoff/poll/receipt отдельно до финальной архитектурной зачистки | — | гейт: ЛПР |
| ~~65~~ | Семантический судья v0. Вызывается при закрытии шага исполнителем-моделью и перед подъёмом ступени на застое или регрессе — прежде чем платить дороже, проверить, туда ли идём. Модель — вендора, противоположного исполнителю; только чтение, без инструментов; вывод до 500 токенов; отдельный машинный JSON и его история. Решения — только по категориям контракта К35, числовая оценка согласованности в v0 не берётся. Два DRIFT подряд — стоп петли; в сессии — через стража хода (шаг 41). Нужен шаг 58: при исполнителях Claude судья — Codex. ЛПР 14.09.2026: «мне кажется, что это есть там суть»; контракт — из разбора ChatGPT того же дня | `sonnet` | К35: тест матрицы решений и контракта ответа |
| 68 | Semantic Plan Review v0 — режим того же semantic judge с `subject=plan`: после принятия/редактирования финального `PLAN.md`, до первой итерации. Reviewer другого вендора, read-only, до 500 токенов; PASS/COMMENTS/DRIFT, `plan_sha256`, до трёх MISSING/REDUNDANT/ORDER_RISK и одна строка NEXT. Reviewer не правит план; DRIFT блокирует запуск, COMMENTS не блокирует | `sonnet` | К38: тест матрицы plan-review, stale hash и блокировки старта · сторона Anthropic |
| 65а | Выпуск 0.10.8: semantic control completion — Semantic Plan Review и полная кросс-вендорная активация; step Semantic Judge transport/scope hardened в 0.10.4 | — | гейт: ЛПР |

Шаги 32, 34, 35 и 36 первой редакции — страж работы, выход по слову ЛПР, ожидание ЛПР и страж судьи на PowerShell — сняты 14.09.2026 решением ЛПР об архитектуре. Их содержание перешло в шаги 39, 41, 42 и 43.

## Цели — этап 0.11: механизм в бинарнике, скил — описание

Основание — постановка AIR-CHG-2026-000092 и слова ЛПР 14.09.2026 о судье: «через хуки, через стража и двухконтурное подтверждение… не должно быть никаких инструкций и указаний сессии. Это должны быть хуки и бинарник, никаких других».

**Ц2.** Механизм air-worker — это бинарник: хук только доставляет ему событие сессии, скил только описывает продукт, и стражи срабатывают в сессии, которая скил не читала.

| Критерий | Цель | Признак достижения | Чем меряется |
|---|---|---|---|
| К11 | Ц2 | каждый хук плагина — обёртка, зовущая `air-worker hook <событие>`; решение принимает бинарник, и решение каждого хука проверено тестом Go | проверка `тесты бинарника` · тест `TestCriterion11Pending` |
| К12 | Ц2 | legacy session-governance полностью снят после готовности binary hooks/guard: файла `skills/woody/hooks/mode.ps1` нет в поставке, shipped hooks/adapters его не вызывают и session lifecycle идёт через binary | проверка `тесты бинарника` · тест `TestCriterion12Pending` |
| К13 | Ц2 | учёт субагентов сводится: старт без остановки и остановка без старта названы, а не теряются молча | проверка `тесты бинарника` · тест `TestCriterion13Pending` |
| К14 | Ц2 | `SKILL.md` — короткий необязательный discovery/help-слой; механизм и решения не меняются при его отсутствии или незагрузке | проверка `тесты бинарника` · тест `TestCriterion14Pending` |
| К15 | Ц2 | adapters доставляют событие в binary без зависимости от skill; прямой EXE-вызов используется, если host поддерживает stdin/exit code, иначе остаётся тонкая transport-обёртка без логики | проверка `тесты бинарника` · тест `TestCriterion15Pending` |
| К30 | Ц2 | роли названы одинаково в постановке, плане, скиле и бинарнике — orchestrator, executor, factual judge, semantic judge, engine, planner, guard; `report` показывает фактическую topology и current tier/host/vendor/model, чтобы профиль ChatGPT→Anthropic→Codex проверялся без чтения исходников | проверка `тесты бинарника` · тест `TestCriterion30Pending` |
| К44 | Ц2 | binary session contract host-neutral: `session declare/status/off/on/forget` требуют явные `principal` + `session key`, не читают `CLAUDE_CODE_SESSION_ID`/другой host-specific session env и разделяют namespaces разных identity на одном продукте | проверка `тесты бинарника` · тест `TestCriterion44Pending` |
| К63 | Ц2 | host adapters только доставляют explicit identity в binary: Claude/Codex и generic external/GPT envelope передают `principal` + `session key` без policy/state; полная GPT Chat+RDC execution semantics остаётся шагом 69 | проверка `тесты бинарника` · тест `TestCriterion63IdentityAdapterPassThroughPending` |
| К46 | Ц2 | status показывает installed plugin/cache/CLI/tray, фактический Claude/Codex config/profile path и версию hooks/runtime, реально загруженную текущей host-сессией; multiple profiles названы явно, а если пакет новее active hooks generation — требуется restart | проверка `тесты бинарника` · тест `TestCriterion46Pending` |
| К62 | Ц2 | mutable runtime state, locks, gates/receipts и ownership namespaced минимум по `(product, principal, session, job)`; две сессии одного продукта могут работать параллельно; global singleton-lock на рабочую сессию запрещён; cross-principal/session mutation отклоняется, а machine-wide install/tray/version metadata не используется как session lock | проверка `тесты бинарника` · тест `TestCriterion62Pending` |

**Ц3.** air-worker работает только по плану с целями, судья меняется только словом ЛПР, а закрытие шага засчитывается механизмом, а не зачёркиванием.

| Критерий | Цель | Признак достижения | Чем меряется |
|---|---|---|---|
| К16 | Ц3 | правка файлов без объявленного продукта с годным планом отклоняется; правка по гейту ЛПР и правка исполнителя петли в корне продукта пропускаются | проверка `тесты бинарника` · тест `TestCriterion16Pending` |
| К17 | Ц3 | контур 1: изменение конфигурации судьи (`run-config.json`) и своего судьи продукта без гейта ЛПР на судью отклонено; скрипты проверок — тесты продукта, их закрытие подтверждает К19 | проверка `тесты бинарника` · тест `TestCriterion17Pending` |
| К18 | Ц3 | контур 2: при расхождении отпечатка согласованного судьи — той же границы, что в К17, — вердикт не выносится: «судья изменён без слова ЛПР», прогресс не засчитывается | проверка `тесты бинарника` · тест `TestCriterion18Pending` |
| К19 | Ц3 | зачёркнутый шаг засчитывается движением, только если проверка его критерия подтвердила закрытие; неподтверждённое закрытие названо в отчёте | проверка `тесты бинарника` · тест `TestCriterionK19ConfirmedClosure` |
| К20 | Ц3 | ход, ждущий ЛПР, называет открытый гейт плана; без такого гейта ход не принимается | проверка `тесты бинарника` · тест `TestCriterion20Pending` |
| К26 | Ц3 | шаг ступени `script` без команды планировщик не предлагает, а петля называет «нечем исполнить» до итерации, не тратя её | проверка `тесты бинарника` · тест `TestCriterion26Pending` |
| К31 | Ц3 | `script:action` с командой закрывается по коду этой команды: 0 — закрытие без подъёма ступени, не 0 — работа остаётся; `script:probe` при коде 0 только обновляет доказательство и шаг не закрывает | проверка `тесты бинарника` · тест `TestCriterion31ScriptActionProbePending` |
| К33 | Ц3 | критерий меряется только машинной ссылкой — проверка судьи, при нужде с селектором теста, или факт реестра; текст без меры в «Чем меряется» делает план негодным. Судья подтверждает каждый критерий его мерой; ненаписанный или упавший тест — критерий не подтверждён и входит в остаток, поэтому расстояние видит работу плана и петля берёт свежий план; цель достигнута, только когда подтверждены все критерии | проверка `тесты бинарника` · тест `TestCriterionK33SelectorGrammar`; проверка `тесты бинарника` · тест `TestCriterionK33MissingSelectorFails` |
| К34 | Ц3 | вердикт двигателя действует и в сессии: при ESCALATE ход сессии не принимается так же, как останавливается петля, — пока причина не вынесена гейтом в план | проверка `тесты бинарника` · тест `TestCriterion34Pending` |
| К35 | Ц3 | судья двухконтурный: фактический — код, семантический — модель другой природы, чем исполнитель шага. Семантический отвечает по короткому контракту: вердикт PASS, DRIFT или NOT_PROVEN; шаг сделан — да, нет или частично; уход — none, minor или major; до трёх фактов-доказательств; одна строка «дальше». На вход — пакет: цель, критерии, шаг, дифф, результаты проверок, заявление исполнителя. Решение — пересечение: код FAIL — FAIL; код PASS и семантика PASS — PASS; семантика DRIFT — стоп без подъёма ступени; NOT_PROVEN или судья недоступен — «не проверено». Семантический судья ничего не правит | проверка `тесты бинарника` · тест `TestParseSemanticVerdictStrict`; проверка `тесты бинарника` · тест `TestCombinedAcceptanceMatrix`; проверка `тесты бинарника` · тест `TestIntermediateReleaseProfileAnthropicExecutorCodexJudge` |
| К38 | Ц3 | финальный `PLAN.md` до первой итерации проходит Semantic Plan Review той же ролью semantic judge, но с `subject=plan`; reviewer другого вендора, read-only, результат привязан к `plan_sha256`. PASS и COMMENTS разрешают запуск, DRIFT блокирует, изменение плана делает review устаревшим | проверка `тесты бинарника` · тест `TestCriterion38Pending` |
| К40 | Ц3 | `run-config.json -> plan` — единственный активный PLAN; goals/report/drift/loop получают один PlanState. Gate определяется только явным metadata; `GATED` отделён от UNKNOWN/NOT_PROVEN, executable distance и число LPR gates показываются раздельно, закрытые gates не считаются work | проверка `тесты бинарника` · тест `TestCriterion40Pending` |
| К41 | Ц3 | `air-worker validate` до loop проверяет PLAN и всю цепочку tier → ladder → runner → tool, точную грамматику таблицы, уникальность шага и команду `script`; ошибка называет строку и причину. Dev/noncanonical dependency называется и предлагается установленный путь; после обновления та же проверка называет нужную миграцию, PLAN сама не переписывает | проверка `тесты бинарника` · тест `TestCriterion41Pending` |
| К51 | Ц3 | factual judge инвентаризует selectors каждой проверки один раз за judge-run и переиспользует результат: для Go — один inventory по нужным packages, для pytest — один collect, для других runner — детерминированный inventory. Отсутствующие selectors не запускают отдельный широкий процесс; число process starts не растёт линейно от количества будущих тестов | проверка `тесты бинарника` · тест `TestCriterion51Pending` |
| К43 | Ц3 | `script:action` и `script:probe` различаются: exit 0 закрывает только action/`close_on_exit_0`, probe никогда не закрывает work-step; loop умеет штатно остановиться после одного work-step (`--once`/`--max-work-steps 1`) | проверка `тесты бинарника` · тест `TestCriterion43Pending` |
| К48 | Ц3 | административные изменения PLAN выполняются командами binary `close-step`, `reopen-step`, `annotate-step`, `split-step` поверх одного PlanState, а не ручной Markdown-правкой | проверка `тесты бинарника` · тест `TestCriterion48Pending` |
| К49 | Ц3 | acceptance имеет тип `unit`, `integration-local`, `live-machine`, `reboot` или `LPR`; unit не закрывает шаг, если критерий требует live/reboot/LPR | проверка `тесты бинарника` · тест `TestCriterion49Pending` |
| К56 | Ц3 | Codex semantic packet на Windows передаётся вне argv: stdin является основным transport, допустим управляемый temp-file fallback; длинный packet не проходит через `codex.cmd` аргументом и сохраняет read-only/output-schema semantics | проверка `тесты бинарника` · тест `TestCriterion56Pending` |
| К57 | Ц3 | semantic review work-step получает отдельный step factual verdict и состояния только критериев этого шага; открытые будущие критерии продукта не могут сделать выполненный текущий шаг factual FAIL, но общий factual judge остаётся финальным судьёй всей цели | проверка `тесты бинарника` · тест `TestCriterion57Pending` |
| К58 | Ц3 | semantic judge доступен standalone read-only командой для loop и внешнего executor одинаково: тот же packet/schema/history, выбор opposite vendor и коды результата без запуска второй исполнительской модели | проверка `тесты бинарника` · тест `TestCriterion58Pending` |
| К59 | Ц3 | distance не считает одну обязанность дважды: факт, уже являющийся мерой критерия, дедуплицирован против legacy `min_facts`; report отдельно показывает executable work, LPR gates и technical unknowns и предупреждает о legacy overlap | проверка `тесты бинарника` · тест `TestCriterion59Pending` |
| К60 | Ц3 | PLAN lint не допускает silent mismatch ссылок: диапазон `К4-К17`/`К4–К17` либо детерминированно разворачивается, либо отклоняется; поддержана согласованная иерархическая форма ID; один критерий у последовательных work-steps выдаёт warning/error до loop | проверка `тесты бинарника` · тест `TestCriterion60Pending` |
| К61 | Ц3 | selector measure умеет package/scope и отдельные select cwd/env/tmp; effective command/context виден в JSON. Исходы различают package missing, test missing, test failed и infrastructure unavailable/transient lock; AirWorker не маскирует product-test failure автоматическим retry | проверка `тесты бинарника` · тест `TestCriterion61Pending` |

**Ц4.** Исполнитель виден числом, оркестрацию ведёт механизм, а недоступность исполнителя называется причиной, а не ступенью вверх.

| Критерий | Цель | Признак достижения | Чем меряется |
|---|---|---|---|
| К21 | Ц4 | отчёт печатает ходы ведущей, запуски субагентов, итерации петли и расход и отличает «двигателю нечего делать» от «двигатель не звали» | проверка `тесты бинарника` · тест `TestCriterion21Pending` |
| К22 | Ц4 | режим оркестрации включён по умолчанию у сессии с объявленным продуктом; при включённой оркестрации ход, в котором код продукта изменила ведущая без субагента и без итерации петли, не принимается; правка плана ведущей разрешена | проверка `тесты бинарника` · тест `TestCriterion22Pending` |
| К23 | Ц4 | недоступный исполнитель назван причиной и временем (лимит вендора, нет входа), ступень при этом не поднимается | проверка `тесты бинарника` · тест `TestCriterion23Pending` |
| К24 | Ц4 | планировщик берёт токен тем же путём, что исполнитель петли | проверка `тесты бинарника` · тест `TestCriterion24Pending` |
| К42 | Ц4 | planner/judge/executor до запуска любой долгой команды создают durable job receipt: `job_id`, PID, `started_at`, `status`, `output_path`, product/principal/session/step/operation; transport/RDC timeout означает `RUNNING`, а не failure; повторный запуск блокируется только для того же owned job/scope и не блокирует независимую соседнюю сессию | проверка `тесты бинарника` · тест `TestCriterion42Pending` |
| К45 | Ц4 | `report/status` имеет cached/no-judge и informational/no-fail режимы: навигация не запускает тяжёлый judge и открытая цель не ломает shell-цепочку информационного режима; full refresh явный/fingerprint-aware, все режимы показывают один PlanState | проверка `тесты бинарника` · тест `TestCriterion45Pending` |
| К50 | Ц4 | developer bootstrap/recovery одной командой показывает version → canonical PLAN → validate/goals → fast report → git/dependency health → объявленные recovery anchors; core только проверяет anchor, продуктовый snapshot не реализует | проверка `тесты бинарника` · тест `TestCriterion50Pending` |
| К54 | Ц4 | `air-worker judge -json` выдаёт id/goal/selector/measure/state/duration/reason/receipt каждого критерия и timings base checks, inventory и selectors; текстовый judge/report строятся из той же структуры, чтобы источник деградации был виден без анализа процессов | проверка `тесты бинарника` · тест `TestCriterion54Pending` |
| К55 | Ц4 | `orchestration.enabled`/CLI override означает доказанный native Agent lifecycle, а не строку prompt: AirWorker требует N фактических Agent tool starts, различает started/completed/blocked, пишет counts/ids в measurement и не объявляет orchestrated success при нуле или недостающих агентах | проверка `тесты бинарника` · тест `TestCriterion55AgentEvidence` |

**Ц5.** Выпуск новой архитектуры не ломает соседние продукты, по которым уже идёт петля.

| Критерий | Цель | Признак достижения | Чем меряется |
|---|---|---|---|
| К25 | Ц5 | перед установкой проверена годность планов продуктов, по которым идёт петля; установка не останавливает исполнителей петли | проверка `тесты бинарника` · тест `TestCriterion25Pending` |
| К36 | Ц5 | установка, которая не состоялась, возвращает не ноль и называет причину; успехом считается только установка, после которой `air-worker version` по имени отвечает поставленной версией | проверка `тесты бинарника` · тест `TestКодОтказаДоходитДоПроцесса`; проверка `тесты бинарника` · тест `TestStatusИсточникУстановленногоСовпалСКэшем` |
| К37 | Ц5 | установка и обновление air-worker — только штатным путём из GitHub для Claude и Codex; бинарник принимается только из их canonical plugin cache, downgrade/рабочее дерево/каталог-маркетплейс отклоняются, `install -status` называет host/source/version/SHA и нарушения | проверка `тесты бинарника` · тест `TestИсточникGitHubКэшСтавит`; проверка `тесты бинарника` · тест `TestПонижениеВерсииБезПравОтказ`; проверка `тесты бинарника` · тест `TestCodexGitHubCacheAllowed`; проверка `тесты бинарника` · тест `TestCodexWrongMarketplaceDenied`; проверка `тесты бинарника` · тест `TestCodexOnlyStatusAcceptsMatchingSHA` |
| К52 | Ц5 | distribution identity не ограничивается SemVer: installed status знает git/revision snapshot; изменение поставляемого plugin payload при неизменной distribution identity блокируется release-check. Ситуация «marketplace commit новее, а update отвечает already latest из-за того же version» должна быть либо невозможна, либо явно названа с штатным способом обновления | проверка `тесты бинарника` · тест `TestCriterion52Pending` |
| К53 | Ц5 | `air-worker selfcheck` работает из installed plugin cache/live runtime без source-tree checkout: проверяет manifest agreement, GitHub source, installed revision/version, cache/live SHA, фактические Claude/Codex config dirs и предупреждает о multiple-profile ambiguity; при нарушении возвращает неноль | проверка `тесты бинарника` · тест `TestCriterion53Pending` |

**Ц6.** air-worker кросс-платформенный: работает и под Claude Code, и под Codex; планировщик — дорогая модель вендора исполнителей, семантический судья — другой природы.

| Критерий | Цель | Признак достижения | Чем меряется |
|---|---|---|---|
| К27 | Ц6 | планировщик — дорогая модель того же вендора, что исполнители: исполнитель верхней ступени лестницы продукта, Claude или Codex; вендора и модель выбирает бинарник по лестнице | проверка `тесты бинарника` |
| К28 | Ц6 | исполнитель Codex исполняет шаг наравне с Claude: пишет в корне продукта, не получает токен другого вендора, расход и ходы берутся из его событий | проверка `тесты бинарника` |
| К29 | Ц6 | плагин air-worker ставится и в Claude Code, и в Codex из одного дерева: манифесты согласованы, сообщения проверки читаемы | проверка `плагин каноничен и переносим` |
| К39 | Ц6 | GPT Chat + RDC — третий host adapter без отдельной логики: binary выдаёт task packet для текущего шага, GPT Chat исполняет его через RDC, после чего тот же factual judge и Anthropic semantic judge оценивают результат. Один packet проектируется под один инструментальный прогон: целевой объём ≤20 минут кодинга, жёсткое окно 20–26 минут; более крупный шаг binary/план обязан заранее разделить. Рабочий профиль чата — GPT-5.6 Sol, high reasoning | проверка `тесты бинарника` · тест `TestCriterion39Pending` |
| К47 | Ц6 | external/manual executor — first-class: фактические host/vendor/model/effort и job_id входят в measurement; AirWorker принимает внешний результат через тот же factual/semantic judge и не запускает вторую модель сам | проверка `тесты бинарника` · тест `TestCriterion47Pending` |

| № | Шаг | Ступень | Судья |
|---|-----|---------|-------|
| 42 | Решение стража работы в бинарнике с исключениями: гейт ЛПР и исполнитель петли в корне продукта с годным планом правку не теряют. Образец — первая редакция на PowerShell | `sonnet` | К16: тест Go на отказ без продукта и на два исключения |
| 43 | Контур 1 защиты судьи в бинарнике: правка `run-config.json` и своего судьи продукта — только по гейту ЛПР на судью | `sonnet` | К17: тест Go на правку без гейта судьи и с ним |
| 44 | Контур 2: бинарник хранит отпечаток согласованного судьи и при расхождении не выносит вердикт, а называет его | `opus` | К18: тест Go на подменённый судья и на несчитанный прогресс |
| 45 | Решения стража кодировки и стража режима перенесены в бинарник — вторых реализаций правил не остаётся; canonical run-config/goal/PLAN/state создаются или нормализуются binary/детерминированной utility, не inline PowerShell | `sonnet` | К11: тест Go на решение каждого из двух событий |

| 47 | Тесты хуков переписаны на сквозную доставку: событие подаётся обёртке на stdin, в том числе в сессии без загруженного скила | `sonnet` | К15, К16, К22: прогон обёрток в `тесты хуков` |
| 50 | Недоступный исполнитель называется причиной и временем, ступень при этом не поднимается | `sonnet` | К23: тест Go на лимит вендора и на отсутствие входа |
| 51 | Планировщик берёт токен тем же путём, что исполнитель петли — «Not logged in» на машине с работающей петлёй не воспроизводится. Исправлено в исходнике, живой прогон 14.09.2026 взял токен | `sonnet` | К24: проверка `планировщик разбивает одним дорогим вызовом и не отнимает план у человека` |
| 52 | Инвариант skill объявлен тестом: без `SKILL.md` plugin/binary/guards продолжают работать; отдельно держится малый предел длины skill | `sonnet` | К14, К15: тест без skill + тест превышения предела |
| 53 | `SKILL.md` сокращён до 10–30 строк discovery/help: что такое Air Worker, зачем он нужен, команды — ссылкой на `air-worker help`, а не списком, который разойдётся с бинарником; явная строка «правила и состояние — в binary». Никаких грамматик, историй дефектов, политик и текстов отказов | `sonnet` | К14: короткий необязательный skill проходит тест предела и тест отсутствия |
| 54 | Установка проверяет годность планов продуктов, по которым идёт петля, и не останавливает их исполнителей | `sonnet` | К25: тест Go на негодный план соседа и на живого исполнителя |
| 55 | Отсутствие управляющей логики вне binary проверяется поставкой: в hooks нет `.ps1`, adapters не содержат решений, skill необязателен, `hooks.json` не хранит исторические трактаты/политику. Второй реализации механизма в поставке нет. Старые движки `skills/woody/scripts` (woody, judge, planner, goal-drift) и `skills/woody/lib` сняты вместе со сверкой равенства в `tools/check-binary.ps1`, для которой они держались эталоном. `skills/woody/capabilities.json` снят: команды печатает `air-worker help`. Движок Python `skills/run-worker-task` — гейт 70 | `sonnet` | К11, К12, К14: счёт файлов/строк и тест plugin без skill |
| 70 | Движок Python `skills/run-worker-task` — вторая точка входа сессии мимо бинарника. `docs/BOUNDARY.md` и `tools/check-boundary.ps1` объявляют его входом для работы, а `air-worker-codex/scripts/run_worker.py` зовёт его `worker.py`. По архитектуре «Binary решает» его место — вне поставки air-worker. Снять его — решение ЛПР, потому что затронут продукт air-worker-codex; до решения шаг 55 его не трогает. Предложение Anthropic 15.09.2026: снять после перевода air-worker-codex на исполнителей бинарника | — | гейт: ЛПР |
| ~~58~~ | Исполнитель Codex наравне с Claude: песочница — запись в корне продукта (планировщику — только чтение); процесс Codex не получает токен Claude из общего `runnerEnv`; задание называет возможности своего исполнителя; разбор событий отличает предупреждение от отказа. Найдено ChatGPT 14.09.2026 (`docs/GPT_OPENAI_AUDIT_2026-09-14.md`), правка отложена патчем. До этого шага петлю с исполнителем Codex не запускать: в 0.10.0 он только на чтение и получает токен Claude | `sonnet` | К28: тест аргументов и окружения вызова исполнителя Codex · сторона GPT |
| ~~59~~ | Планировщик — дорогая модель вендора исполнителей: `air-worker plan` зовёт исполнителя верхней ступени лестницы, Claude или Codex. В релизе 0.10.0 он всегда зовёт `claude`; рабочее дерево переведено на vendor-aware выбор верхней ступени. Решение ЛПР 14.09.2026: «планировщик тоже должен быть моделью того же вендора, но просто дорогой»; независимость — у семантического судьи (шаг 65) | `sonnet` | К27: `TestPlannerUsesTopExecutorVendor`, `TestCodexPlannerSandboxIsReadOnly`, полный `go test ./...` — PASS · исполнитель: ChatGPT |
| ~~60~~ | Сверка манифестов Claude и Codex в `tools/check-plugin.ps1`: кириллица новых строк записана знаками «?» — перезаписать в UTF-8 с BOM | `haiku` | К29: проверка `плагин каноничен и переносим` с читаемыми сообщениями · сторона GPT |
| 76 | Штатные операции PlanState: `close-step`, `reopen-step`, `annotate-step`, `split-step`; административное изменение состояния идёт через binary, ручная Markdown-правка остаётся только аварийным/dev режимом | `sonnet` | К48: `TestCriterion48Pending` |
| 77 | Acceptance types: `unit`, `integration-local`, `live-machine`, `reboot`, `LPR`; критерий хранит требуемый тип, и unit не может закрыть live/reboot/LPR шаг | `sonnet` | К49: `TestCriterion49Pending` |
| 78 | Developer bootstrap/recovery guard: одна команда version → canonical PLAN → validate/goals → fast report → git/dependency health → recovery anchors. Продуктовый recovery snapshot остаётся у продукта; core только проверяет объявленный anchor | `sonnet` | К50: `TestCriterion50Pending` |
| 56 | Выпуск 0.11.0: бинарная архитектура, двухконтурный судья, краткий скил | — | гейт: ЛПР |

## Почему именно такие ступени

**Шаги 1, 2, 7, 8 — `script`.** Заведение конфигурации, объявление проверки, счётчик
застоя и сверка воспроизводимости детерминированы и проверяются точным выходом. Модель
здесь была бы дефектом по прямому правилу скила.

**Шаги 3, 4, 6 — `sonnet`.** Обычный кодинг с названной причиной: прогнать лестницу по
ступеням, завести формат файла задания, замкнуть цикл двигателя. Ни архитектурного
суждения, ни разбора противоречий здесь нет.

**Шаг 5 — `opus`.** Единственный, где нужно суждение: как резать чужую цель на шаги и по
какому признаку назначать ступень. Ошибка здесь дороже остальных, потому что
тиражируется на все последующие прогоны.

**Шаги 9 и 10 — гейты ЛПР.** Выпуск и смена упаковки решением не сессии: первое — норма
контура (`AIR_VIBECODING` § 2.7), второе — прямое слово ЛПР 12.09.2026 («сначала плагин;
когда поймём, что работает пристойно, тогда перейдём на бинарник»).

**Шаги 19-21 — пласт, вскрытый вопросом ЛПР 13.09.2026 «а как бинарник взаимодействует с
ЛЛМ».** Вопрос оказался не про механизм, а про продукт целиком.

Взаимодействие с моделью живёт в ДВУХ местах, и только одно в бинарнике. Механизм — петля
и планировщик — зовёт вендорский CLI из Go и читает его JSON. Исполнение ЗАДАЧ — шесть
ступеней лестницы, локальная qwen, бесплатные модели OpenRouter, codex — живёт в
`ladder.py` и ходит через `llm-queue`, которую мы не переписываем.

Шаг 19 сводит их в одно место, и он дорогой: это `ladder.py` плюс пять модулей вокруг
`worker.py` плюс граница с чужой очередью. Шаг 20 отвечает на вопрос AIR-ENV-002, который
не про упаковку: плагин был не только способом доставки, но и ТОЧКОЙ ВХОДА — через него
сессия видела скилы и могла позвать продукт. Шаг 21 — гейт, потому что «должно ли
приложение быть самодостаточным» это решение о назначении продукта, а не о его коде.

**Гейт 21 снят с ЛПР решением 13.09.2026, и это сверка с целью, а не своеволие.** Вопрос
звучал «должен ли бинарник быть самодостаточным», но фактически означал «переписать ли в
Go клиент СОСЕДНЕГО продукта `llm-queue`, у которого свой объявленный CLI». Это
архитектурное решение, а не решение ЛПР: его — права, окна у машины, деньги и выпуск
наружу. Ответ дан фактами: свой клиент очереди был бы второй реализацией одного правила на
уровне продуктов, то есть той же бедой, которую мы весь день чинили на уровне функций.

Поэтому шаг 19 закрыт не переносом, а ОБЪЯВЛЕНИЕМ ГРАНИЦЫ — и это честнее: перенос
выглядел бы работой и создал бы расхождение, которое обнаружилось бы не там, где сделано.

**Шаг 23 — единственное, чего механизм о себе не доказал.** Судья зелёный, равенство
реализаций проверено, обе площадки развёрнуты, но в журнале `steps.jsonl` **ни одной
настоящей итерации**: обе строки сухие, расход `null`. То есть доказано, что механизм
устроен верно, и не доказано, что он доводит до результата ЧУЖУЮ задачу.

Закрыть это может только практическая задача, и выдумать её нельзя: задача, придуманная
самим механизмом под себя, проверяет механизм ровно в той мере, в какой он её и сочинил.

**Поэтому шаг 23 — гейт, а не отложенная работа, и различие здесь не формальное.** Работу
откладывают, когда есть чем заняться и не хочется; здесь же ВХОД шага физически отсутствует
у сессии. Собственный план air-worker пройден, судья зелёный — значит петля на нём
остановится словами «цель уже достигнута», не сделав ни одной итерации. Чтобы итерация
состоялась, нужен незакрытый шаг; выдумать его — это и есть сочинить себе проверку.

Площадки соседей на эту роль не годятся по той же причине: их продукты ведут они, и
чужая задача, взятая без их решения, проверила бы не механизм, а мою готовность зайти на
чужое поле.

**Шаг 25 и снятый с него гейт — разбор ошибки диагноза, стоившей одного хода.**

Первый настоящий прогон стоил $7.33 и не изменил ни одного файла. Я заключила, что
отцеплённый исполнитель писать не может в принципе, и вывела это на гейт ЛПР: мол, нужно
полное отключение проверок прав. ЛПР не согласился и велел перепроверить, указав, что
сессия, дорабатывавшая ВЕРУ, с отцеплёнными прогонами работала нормально.

Он был прав. Перепроверка замерами показала: исполнитель ПИШЕТ ФАЙЛЫ, и пишет их в тех же
каталогах и с теми же правами. Неверной была ПРОБА: `--allowed-tools` — флаг переменной
арности, и раздельные аргументы («Write Edit») он разбирает не так, как ожидается. Список
через запятую, как в проверенной ветви того же дня, работает.

Урок дороже самой правки и попадает в тот же ряд, что и остальные за сутки: **опыт,
поставленный неверно, доказывает не то, что проверяли.** Я уже ловила это в тот же день на
изоляции BOM, где правку вносил сам PowerShell, — и повторила через несколько часов.

Отдельный исход «исполнителю не дали прав» в коде остаётся: он верен по существу и нужен,
если права однажды действительно отнимут. Но диагнозом по умолчанию он больше не служит.

## Что сознательно не в плане

- Переписывание `llm-queue` и `ladder.py`: граница docs/BOUNDARY.md остаётся (постановка 000092, раздел 5).
- Смена упаковки: плагин остаётся способом доставки.
- Работа в продуктах соседей (ASW, ВЕРА) — только совместимость выпуска с ними.
- Смена первичного ключа в `smeta_engine` и скачивание архивов ФСНБ — постановка 000091, раздел 4.
- Проверка гейтов командой. Предложение AIR-ENV-002 14.09.2026, замер: 6 ложных гейтов из 14. Отложена решением ЛПР: «не усложняем пока… усложнять будем позже». Вернуться, когда петля стабильно доводит шаги до подтверждённого закрытия (К19). Метрика у продуктов с накопленными гейтами после этого честно ухудшится: у AIR-ENV-002 снятие двух ложных гейтов подняло расстояние с 1 до 2.
- Семантическая проверка всей цели перед выпуском — после v0 semantic judge и plan-review (шаги 65/68); это отдельная будущая функция, не путать с утверждённым review плана до исполнения.
- Своя подписанная лента обновлений по образцу AIR Kill Switch: манифест в ветке `update-feed`, SHA-256 и две подписи Ed25519, транзакционная установка с откатом. С AIRKS air-worker совпадает в принципе: только GitHub, сверка версии и SHA-256 после установки, без молчаливого понижения, прямая переустановка — только с правами. Отличие одно: транспорт — маркетплейс плагинов Claude, доверие — репозиторий в `known_marketplaces.json`. Сверка Anthropic при шаге 66, 15.09.2026. Пока механизм не работает, не усложняем; вернуться, если понадобится канал вне маркетплейса или подпись выпуска.
Перенос механизма в бинарник прежде стоял здесь (шаг 10 — «только решение, не работа»). С 14.09.2026 он в плане: этап 0.11, по слову ЛПР.

## Эксплуатационный backlog по боевым прогонам 14–15.09.2026

Этот раздел обязателен и не может раствориться после текущей сессии. Источники: реальные прогоны AirWorker 0.10.1 на AIR-ENV-002, ASW/SRVLM01 и ручные GPT-5.6 Sol + RDC сессии.

**Интеграция в исполняемый план 15.09.2026.** Этот раздел ниже — доказательная история, не второй source_of_truth. Исполнение идёт только по numbered steps выше.

**AirLegal backlog 15.09.2026 — три прогона:** distribution identity/selfcheck остаются в 79; orchestration 0.10.2 с declared=2/actual=0 закрыта шагом 80 в 0.10.3; новый реальный дефект — Codex semantic packet через argv превышает Windows cmdline, а global factual code загрязняет step review будущими критериями. Это P0 шаги 81–82.

**ASW + AirVera backlog разобраны 15.09.2026:** ASW дал selector/package performance, ranges/IDs, GATED, standalone semantic, timings/lint; AirVera — duplicate factual distance, informational report, selector context, Windows cleanup lock classification, multiple Claude profiles и active-hooks/restart. Они поглощены 71/72/75/79/39 и критериями К59–К61, без параллельного второго плана.

| Кластер backlog | Исполняемый контур | Приоритет |
|---|---|---|
| Windows-safe semantic transport | 81 / К56 | 0.10.4 |
| step-scoped factual + standalone semantic review | 82 / К57, К58 | 0.10.4 |
| same-version identity + installed/profile selfcheck | 79 / К52, К53 | 0.10.5 |
| canonical PLAN + GATED + deduplicated distance | 71 / К40, К59 | 0.10.5 |
| validate/lint + ranges/IDs + batched/package selector context | 72 / К41, К51, К60, К61, К26 | 0.10.5 |
| long-running RUNNING receipt + защита от duplicate start | 73 / К42 | 0.10.5 |
| action/probe + один безопасный work-step | 74 / К31, К43 | 0.10.5 |
| cached/informational report + structured judge/timings | 75 / К45, К54 | 0.10.5 |
| principal/session identity + multi-session isolation | 39 + 83 / К44, К62 | 0.10.5 |
| external/manual executor GPT+RDC | 69 / К39, К42, К47 | 0.10.7 |
| plan operations / acceptance types / bootstrap-recovery | 76–78 / К48–К50 | 0.11.0 |
| encoding contract | 45 + 55 | 0.11.0 |
| product-specific recovery snapshot | не core: только recovery anchor в 78 | не расширять AirWorker |

**Разбор последнего AirLegal orchestration отчёта.** `requested=2/actual=0` относится к 0.10.2 и уже закрыт 0.10.3: К55 требует реальные Agent starts/results и strict count. Workaround с двумя отдельными Claude-процессами/worktrees остаётся допустимым экспериментом, но не становится вторым orchestration engine. Новая работа только по двум доказанным дефектам: Windows-safe Codex transport (81) и step-scoped/standalone semantic review (82).

**Разбор ASW backlog.** GATED и executable distance → 71/К40; ranges/ID/multi-step lint → 72/К60; selector batching/package scope/outcome taxonomy → 72/К51/К61; standalone semantic → 82/К58; fast report/timings → 75/К45/К54.

**Разбор AirVera backlog.** duplicate `min_facts`+criterion distance → 71/К59; informational report → 75/К45; selector cwd/env/tmp и Windows lock classification → 72/К61; multiple Claude profiles → 79/К53; active hooks generation/restart → 39/К46. Автоматический retry product tests из-за Windows lock не вводится: сначала классифицируется infrastructure/transient, чтобы не маскировать реальный FAIL. AirVera = consumer/evidence only; its single loop/lifecycle does not define singleton architecture for AirWorker/AirBot; findings are generalized into multi-session criteria.


- **P0: единый канонический PLAN.** `run-config.json -> plan` — единственный канон; второй `source_of_truth` запрещён. Остальные планы только historical/supporting.
- **P0: vendor-neutral principal/session/product declaration.** `air-worker session declare` получает explicit product + principal + session key; host-specific session IDs — только вход adapters, не ключ внутреннего governance.
- **P0: multi-session isolation.** Ни один work/session lock не глобален. State/receipts/gates/ownership scoped by product+principal+session(+job); две сессии одного продукта должны пройти live concurrency smoke, а cross-session mutation — fail-closed.
- **P0: validate/goals до loop.** Проверять PLAN -> tier -> ladder -> runner -> tool; для `script` обязательна команда. План с несуществующей ступенью не может считаться годным.
- **P0: first-class gated state.** Admin/reboot/login/физическое действие ЛПР переводятся штатной командой в gated/awaits, не увеличивают stall и не запускаются loop.
- **P0: long-running job receipt.** Planner/judge/executor до запуска создают `job_id`, PID/started_at/status/output_path; RDC timeout означает RUNNING, а не failure. Повторный запуск не делается, пока job жив.
- **P0: единственный PlanState.** `open_gates = Gate && !Done`; Gate существует только по явному `гейт: ЛПР`/metadata, не выводится из tier или зачёркивания; goals/report/drift/loop используют один объект состояния. Закрытые gates показываются отдельно.
- **P1: явная семантика script-step.** Разделить `script:action` и `script:probe` либо ввести `close_on_exit_0`; probe с кодом 0 не закрывает шаг.
- **P1: ограничение одного рабочего шага.** Нужен `loop --once` / `--stop-after-step` / `--max-work-steps 1` для безопасной работы на продуктивных машинах.
- **P1: штатные операции с PLAN.** `close-step`, `reopen-step`, `annotate-step`, `split-step`; административное изменение состояния делает AirWorker, а не ручная Markdown-правка.
- **P1: отдельный plan lint.** Точная диагностика по строкам: число колонок, номер шага, `script :: command`, неизвестный tier, отсутствующий runner/tool.
- **P1: fast/full report.** Дешёвый status не должен прогонять тяжёлый judge без необходимости; полный refresh вызывается явно или по fingerprint входов judge.
- **P1: external/manual executor.** GPT Chat + RDC и другие внешние исполнители — first-class mode: фактические vendor/model/effort попадают в measurement, AirWorker не запускает вторую модель сам.
- **P1: plugin/hook active-version status.** Одна команда показывает installed plugin/cache/CLI/tray и версию hooks, с которой стартовала текущая host-сессия, плюс необходимость restart.
- **P1: developer bootstrap/recovery guard.** Одна команда: version -> canonical PLAN -> goals/validate -> report/status -> git status -> dependency health -> recovery anchors.
- **P1: post-update compatibility guard.** После обновления AirWorker проверяет активный продукт на новые обязательные контракты и называет миграцию; PLAN автоматически не переписывает.
- **P1: encoding contract.** Канонические `run-config.json`, `goal.json`, PLAN и state создаются/нормализуются binary или маленькой детерминированной utility, не inline PowerShell.
- **P2: acceptance types.** `unit`, `integration-local`, `live-machine`, `reboot`, `LPR` — unit не закрывает шаг, если нужен live acceptance.
- **P2: dev-path dependencies.** `goal.json` не должен молча ссылаться на dev-бинарник; validate называет отсутствующий/нештатный путь и предлагает установленный `air-worker` из PATH.
- **Продуктовый recovery snapshot.** `airlegal recovery snapshot` относится к AirLegal, не к core AirWorker; AirWorker только проверяет объявленный recovery anchor, если продукт его требует.

### Инвариант оптимизации GPT Chat + RDC

Инструментальный прогон чата — примерно 20–26 минут. После каждого прогона обязательны два независимых шага обратной связи:
1. Свериться с текущими goal/PLAN/current step и явно вернуться в основную цель.
2. По фактам прогона оптимизировать собственную тактику: порядок инструментов, размер пакета, проверки, способ редактирования и handoff.
3. Отдельно оценить, накоплены ли факты для изменения PLAN; план меняется только если это улучшает путь к цели, а не ради самой оптимизации.
4. Следующий task packet должен укладываться в один инструментальный прогон; прогноз >20–26 минут означает предварительное дробление.
5. После изменения PLAN следующий прогон начинается с повторной валидации и semantic plan review, когда шаг 68 доступен.

Принцип: оптимизация тактики выполняется после каждого инструментального прогона; оптимизация PLAN — только по накопленным фактам. Обе оптимизации подчинены возврату к исходной цели.
**Уточнение ЛПР 15.09.2026:** цель обеих оптимизаций — быстрее прийти к исходной цели. Изменение тактики или PLAN, которое не сокращает путь к цели либо снижает доказуемость/безопасность, оптимизацией не считается.
