//go:build !windows

package main

import (
	"fmt"
	"os"
)

// Трей — вещь оконная и оконно-зависимая. На не-Windows он не собирается в заглушку,
// которая «как бы работает»: продукт живёт на Windows, и молчаливая пустышка была бы
// хуже честного отказа — её приняли бы за работающий значок.
func main() {
	fmt.Fprintln(os.Stderr, "трей air-worker работает только на Windows: значок в области уведомлений — вещь этой ОС")
	os.Exit(2)
}
