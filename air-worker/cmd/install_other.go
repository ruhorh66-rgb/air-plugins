//go:build !windows

package main

import (
	"fmt"
	"os"
)

// Установка описана в терминах этой ОС: пользовательская область, ветвь автозапуска,
// область уведомлений. Собирать заглушку, которая «как бы поставила», нельзя — она
// отвечала бы успехом там, где ничего не произошло.
func cmdInstall(argv []string) int {
	fmt.Fprint(os.Stderr, "установка со значком в трее сделана для Windows: автозапуск и область уведомлений — вещи этой ОС"+lineEnding)
	return 2
}
