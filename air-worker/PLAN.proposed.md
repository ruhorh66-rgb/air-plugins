# PLAN — предложен Планировщиком 13.09.2026 14:12

Постановка: E:\-0-\AIR_OS\04_AI_OPERATIONS\CHANGE_PACKAGES\AIR-CHG-2026-000091_AIR_WORKER_GUARANTEED_RESULT_OBJECTIVE.md
Разбивка сделана ОДНИМ вызовом модели opus:max. Замер: $0.8935, ходов 7.

ЭТО ПРЕДЛОЖЕНИЕ, А НЕ ПЛАН. План правится руками: машина исполняет, человек владеет
разбивкой. Перенеси в PLAN.md то, с чем согласен, — целиком или частями.

| № | Шаг | Ступень | Судья |
|---|-----|---------|-------|
| 1 | Прогнать судью на air-worker и записать базовый вердикт (код, число проверок, факты) в `.woody\baseline-verdict.txt` | `script` | исполнитель: ведущая |
| 2 | Внести судью продукта `claude_judge_run.py` в `judge.checks` файла run-config.json свидетелем: он даёт проверку, вердикт остаётся за judge.ps1 | `sonnet:medium` | исполнитель: субагент |
| 3 | Прогнать судью повторно: число проверок выросло ровно на одну, код прежний, «нечем проверить» отличимо от «не прошло» | `script` | исполнитель: ведущая |
| 4 | Решить, кто выбирает модель шага — лестница run-config.json двигателя или ladder.py продукта; два механизма на один шаг несовместимы, решение записать отдельным документом решения | `opus:medium` | исполнитель: ведущая |
| 5 | Свести лестницу в один источник по решению шага 4; расхождение состава ступеней между run-config.json и ladder.py — отказ | `sonnet:max` | исполнитель: субагент |
| 6 | Написать `tools\check-ladder.ps1`: по steps.jsonl модель вызова равна назначенной ступени, подъём только при неподвижном вердикте, упор в потолок ходов подъёмом не считается | `sonnet:medium` | исполнитель: субагент |
| 7 | Написать `tools\check-planner.ps1`: разбивка сделана одним вызовом, у каждого шага PLAN.md есть ступень из лестницы, и ступени различаются | `sonnet:medium` | исполнитель: субагент |
| 8 | Написать `tools\check-fresh-session.ps1`: каждая итерация в steps.jsonl — отдельный процесс исполнителя со своим идентификатором сессии, а не продолжение одной | `sonnet:medium` | исполнитель: субагент |
| 9 | Написать `tools\check-stall.ps1`: на неподвижном вердикте кампания снимается по budget.stall_runs с записанной причиной, а не жжёт квоту молча | `sonnet:medium` | исполнитель: субагент |
| 10 | Перенести согласованные шаги из PLAN.proposed.md в PLAN.md — разбивкой владеет человек | — | гейт: ЛПР |
| 11 | Запустить первую кампанию двигателя на air-worker до вердикта либо до потолка; результат — непустой steps.jsonl и остановка по одному из объявленных условий | `script` | исполнитель: ведущая |
| 12 | Прогнать check-planner, check-ladder, check-fresh-session и check-stall на steps.jsonl первой кампании; любой отказ возвращает к своему шагу | `script` | исполнитель: ведущая |
| 13 | Заменить консервативные потолки кампании в goal.json (maxIterations, maxMinutes, maxRunsPerDay) числами по замеру первой кампании | `haiku:medium` | исполнитель: субагент |
| 14 | Написать `tools\check-reproducible.ps1`: тот же набор файлов в новой сессии даёт тот же путь к вердикту; расхождение состава шагов или ступеней — отказ | `sonnet:max` | исполнитель: субагент |
| 15 | Прогнать вторую кампанию из чистой сессии и сверить её путь с первой через check-reproducible.ps1 | `script` | исполнитель: ведущая |
| 16 | Снять из docs/IMPLEMENTATION_PLAN.md закрытый дефект «лестница обрывается на ступенях 2 и 3» и вписать пять ролей AUTO-080 с их носителями | `haiku:medium` | исполнитель: субагент |
| 17 | Поднять версию плагина до 0.4.0 в манифесте и CHANGELOG по составу закрытых признаков постановки | `script` | исполнитель: ведущая |
| 18 | Выпустить air-worker 0.4.0 в личный маркетплейс | — | гейт: ЛПР |
| 19 | Утвердить AIR-CHG-2026-000091 (review_status: draft) | — | гейт: ЛПР |
